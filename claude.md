# OE GROUP — AI-POWERED INTEGRATED FM / PROPERTY MANAGEMENT SYSTEM (IWMS)
### Streamlined AI Instructions · Master Build Prompt v3.6 · Step-by-Step Design Workflow
**Classification: Board Confidential · July 2026 · TFML + OEA**
> ### ✅ Locked Scope Decisions (v3.6 — August 2026)
> 1. **Scale:** 100+ properties from day one; architecture must stay flexible and scalable.
> 2. **Funds:** client funds are held in OE Group's **own designated bank accounts**; OE Group manages and authorises disbursement. This is a managed client-funds account + authorisation workflow (not licensed custody/escrow) → keep a **segregated** client-funds account, an in-app segregated ledger, the authorisation workflow, and **daily bank reconciliation**.
> 3. **Approvals:** approval hierarchy and thresholds are **admin-configurable** (add/update approvers and limits via an admin user).
> 4. **Payments:** **Paystack** (Collections + Transfers/remittance) **+ Flutterwave** (FX / international collections) — multi-currency retained.
> 5. **Build model:** in-house **hybrid**, **AI-led end-to-end** — most cost-effective workable model (≈ ₦5.9M–₦8.9M one-time vs ₦11.7M–₦22M human-hybrid).
> 6. **Aidra:** Phase 2 (reporting pilot).
> 8. **The portfolio has a regional structure (v3.3, 29 Jul 2026 board).** Both brands organise properties as **REGION → PROJECT → LOCATION → SITE → PROPERTY → UNIT → ASSET**, regions following Nigeria's geopolitical mapping (North, South, East). Implemented as **one** `org_nodes` table with a materialised path hanging *above* the property register — not five nested tables — because `current_user_property_ids()` is referenced by 42 policy clauses and the **property remains the security anchor**. `properties.site_node_id` is nullable: an unfiled property stays fully operable.
>     - **Order amended to REGION → LOCATION → PROJECT → SITE (v3.4, 31 Jul 2026).** The minuted order placed PROJECT above LOCATION, which meant "Kano" could not be recorded until a project had been invented to contain it. A project happens *in* a place; a place does not happen in a project. Nigeria's cities are seeded as **locations** under the three regions (Abuja, Kano, Sokoto, Kaduna, Jos, Maiduguri, Ilorin, Katsina · Lagos, Ibadan, Benin City, Abeokuta, Akure, Osogbo, Warri · Port Harcourt, Enugu, Owerri, Aba, Onitsha, Awka, Calabar, Uyo, Yenagoa, Umuahia), for **every** live org. **Minuted as an amendment, 3 Aug 2026.**
>     - **The FM/PM builds the tree while filing a property.** Location, project and site are created inline from the property form by anyone holding `hierarchy.write` — a picker that can only select is a dead end for the first property in a new city.
>     - **Assets state their scope.** `assets.scope` ∈ `unit | property | site`. "Shared" is a stated fact, never an absent `unit_id` — a nullable FK used as a meaning produced three live defects in one week, because NULL never matches an `IN` list.
>     - **One resolver, extended.** Node scoping was added *inside* `current_user_property_ids()`. A second scoping mechanism alongside the first is forbidden.
> 9. **Two roles added (v3.3, 29 Jul 2026 board).**
>     - **`executive`** — the **Managing Director** of TFML and the **Managing Partner** of OEA (one role, brand-aware label, exactly as FM/PM). Sees everything finance sees and **co-holds payment approval, including above the threshold**. **Cannot execute a remittance**, add or change a bank account, post to the ledger, or raise the threshold they approve against. *Oversight authorises; finance disburses* — approving against a limit you can lift yourself is not an approval. Enforced in `enforce_payment_transition()`, never as a toggle.
> 16. **Disbursement belongs to finance alone, and never to the approver (v3.4, 8 Aug 2026).** Decision 9 stated *"oversight authorises; finance disburses"* but enforced it only against `executive`. The same critique lands harder on **`admin`**, who can raise the approval threshold, approve beneath it, **and** — until now — release the funds. Two rules, both in the database (`0142`), because decision 7 already names remittance execution a non-delegable control:
>     - **Only `finance_approver` may execute a remittance**, vendor payment or landlord payout. An administrator approves within the threshold and configures it; an executive approves above it; neither releases money.
>     - **The approver of a payment may never also release it** — per payment, per person, not merely per role. This is the control that actually prevents one person paying themselves out, and it is the same maker-checker shape `0082` already uses for two-tier tenant review. It can legitimately refuse a finance approver who approved the payment themselves; that is the rule working, and the answer is a second pair of hands, not a code exception.
>     - **Who executed is now recorded.** `remittances.created_by` was NULL on every row ever written, because both `create_*_remittance` functions stamp `auth.uid()` and both are called through the service-role client, where it is null by definition. The executor is now passed explicitly and required — the one action that moves real money had been the one action with no attributable actor.
>     - Approval above the threshold is **unchanged** (`admin` + `executive`, per decision 9's "co-holds"). The concentration is already broken by the above: an admin who approves can no longer release.
>     - **`regional_manager`** — decentralised FM/PM administration. Everything a facility/properties manager holds, plus inviting **operational** staff, bounded to the region/project/site they are assigned to. Nothing financial, no org-wide read. Admin invitation stays non-delegable (decision 7).
>     - Who may **see** money and the audit trail is now one definition, `oversight_roles()`, rather than the same role array repeated across 18 policies.
> 10. **AI may verify documents; it may never screen (v3.3, 29 Jul 2026 board — amends decision 2 of the OEA expansion).** Tenant applications keep **two-tier human review**. No automated system may **decide, score, rank or recommend** an outcome. Automated **document verification** — extraction, format and consistency checks, completeness, duplicate detection — is permitted as decision *support*, must record findings **against the evidence they came from**, and cannot substitute for the reviewer's own recorded reason.
>     - The NDPA Art. 37 test is whether a decision is **solely** automated with significant effect; refusing housing is significant and a rubber-stamp does not cure it. Hence: findings never conclusions, the reviewer must state their own reason, findings are auditable and contestable, a bias audit on the extraction, and a per-org B9 flag **off by default**.
>     - Special-category data (religion, marital status) stays in the separate `sensitive` column and is **never** sent to a model.
>     - Consent copy gains a line about automated document checks. Statements are stored **verbatim per application**, so existing applicants keep the wording they actually saw.
>     - Sending identity documents to Claude makes Anthropic a **processor** → DPA required (A3); prefer sending extracted text over document images.
> 12. **Every organisation has its own front door; only an operator sees the list (v3.4, 31 Jul 2026).** Each org carries a unique `slug` and is reachable at **`/o/<slug>`** with its own branding on its own sign-in. The **grid of organisation icons** requested for the OE Group home screen sits **behind the operator sign-in** at `/orgs`, not in front of it.
>     - **B1 is the reason.** "A user on one portal must never see the other brand's data **or existence**" — a public grid publishes the entire client list (both brands, the SC client, every landlord org) to anyone who loads the page. A link someone was *given* is not an enumeration; a directory is.
>     - **`org_public_branding(slug)`** is anonymous, takes a slug, returns **at most one row**, and cannot be made to list — wildcards and quotes match literally. An unknown slug and a retired org both answer 404, so the platform cannot be mapped.
>     - **`operator_org_directory()`** gates on `caller_is_operator_admin()` **inside the query**, so a brand administrator receives an **empty set rather than a refusal** — a refusal confirms there is something worth refusing.
>     - Making the grid public later is one line; un-publishing an indexed client list is not. **Any move to a public directory needs a recorded board exception to B1.**
>     - Slugs are unique **among live orgs only** and derived from the **name**, never `delivery_brand` — which says which brand delivers the work, not which organisation this is (two OEA orgs collided on the first attempt).
> 14. **Management-fee model: org default + per-landlord override (v3.4, 1 Aug 2026).** `orgs.management_fee_pct` is the baseline; a landlord may carry a negotiated rate of their own, shown as a diff from the default with a one-click reset — the same pattern decision 7 already uses for the permission matrix, reused rather than reinvented for money. Matches how PM fees are actually negotiated in the Nigerian market; a single fixed org-wide rate does not. **The admin fee stays an org-wide flat placeholder** — real enough for the Day 9 rent roll to show a line for it, deliberately not built out until its shape (ongoing % vs one-time per-tenancy charge) is decided. Whatever rate applies is **snapshotted onto the transaction at collection time**, never referenced live — a later rate change must never silently rewrite a past landlord statement.
> 15. **Rent cadence and notice lead times (v3.4, 1 Aug 2026; minuted 3 Aug 2026).** Rent is billed **annually in advance** — the Nigerian norm, where one or two years up front is ordinary and a monthly cycle is the exception. Renewal notices go out at **90, 60 and 30 days** before a tenancy ends. Both are **per-org configuration** (`orgs.rent_demand_lead_days`, `orgs.renewal_notice_days`, Settings → Lettings), not constants: a commercial portfolio legitimately wants longer notice than a residential one. A notice fires once per (lease, threshold) — the record decides, never the schedule, so a retrying job cannot tell a tenant the same thing three times.
> 13. **UI/UX upgrade is staged, client-facing surfaces first (v3.4, 31 Jul 2026).** A modern, conventional client-serving treatment lands as **Day 8.9** on the public entry surfaces (org launcher, `/o/<slug>` sign-in, tenancy application) — the surfaces a client or prospect actually sees — while the internal dashboard's polish stays in **Day 11**'s existing production UX pass rather than being done twice.

> 17. **A vendor company is not a single login, and registers once (v3.4, 17 Aug 2026).** `vendors.user_id` was one nullable FK and *the* vendor-side permission check in eleven live policies and functions — so a contractor's cleaner, office manager and director shared one password, on the one side of the payment chain we otherwise attribute obsessively. Worse, `accept_invitation` ended with `update vendors set user_id = …`, so inviting a **second** person **evicted the first** — the state `0116` exists to prevent, reached from the other direction.
>     - **`vendor_users` + four fixed capabilities** (`manage_users`, `manage_profile`, `manage_work`, `manage_contracts`), resolved by **`current_user_vendor_ids()`** — the vendor-side twin of `current_user_property_ids()`. Decision 8's rule applies unchanged: one resolver, extended. Reading stays company-wide; **acting** requires the capability.
>     - **Not the permission matrix.** Decision 7's matrix governs OE Group's own staff and is operator-only to edit. A vendor deciding which of *their* staff may invoice is one level below it and must never share its control surface. The four capabilities are fixed by migration and configurable by nobody.
>     - **Registration is tiered.** `standard` (default: CAC, TIN, bank evidence, proof of address — all compulsory) and `enhanced` (adds ownership, directors, tax clearance, audited accounts, director ID), set per vendor by the managing organisation. A thirteen-section form is how a two-man contractor ends up unregistered, not how they get vetted harder.
>     - **Bank details are stated and evidenced, never actionable.** Last four digits plus the bank's own document; finance reads the number off it and registers the payout recipient through `0040b`, whose rule — the number goes to the gateway once and is never stored — is unchanged. **No path exists from the registration into `payout_recipients`.**
>     - **Nothing here gates a payment.** `vendor_registration_state()` reports and refuses nothing — `0161`/`0162`'s lesson about controls nobody asked for applies with full force to money a contractor is owed.
>     - **A vendor known to one brand may introduce themselves to the other (`0165`).** The vendor initiates, addressing the target org **by slug** (`0085`: a link resolves, a dropdown enumerates), consent stored verbatim and withdrawable. The receiving org gets a **copy** — its own vendor, pack and documents — arriving `submitted`, and verifies and approves it itself. Verification status, machine findings and source-org user ids do not cross.
>     - **The offer does not name the source organisation.** The receiving org learns a contractor is registered *elsewhere on the platform*, not with whom — because B1's "or existence" is the whole point, and naming it would need a **recorded board exception**, the same bar decision 12 sets for a public directory. An introduction is consequently the one row in the schema belonging to two orgs, and is audited into **both** trails, each redacted of the other (`0167`).
>     - Scope, decisions and what is still owed: **`docs/VENDOR_SELF_SERVICE_SCOPE.md`**. Verified by `scripts/verify-vendor-self-service.mjs` (41 checks).

> 18. **OEA employs facilities managers too, so FM and PM become two roles (v3.5, 21 Aug 2026).** `facility_manager` was one role wearing a brand-aware label — "Facilities Manager" on TFML, "Properties Manager" on OEA — and `lib/roles.ts` argued that splitting it "would double every RLS policy for no security gain". That held only while no organisation employed both. OEA now staffs facilities managers alongside its property managers: two people, two disciplines, two sets of properties, each signing off their own work, **sharing a brand** — so a brand-aware label can no longer tell them apart.
>     - **The split cost one array element, not thirty predicates**, because `fm_roles()` has been the single operational resolver since `0078a`. That is decision 8's "one resolver, extended" paying out a second time. Only 5 policies and 10 functions still named the role literally, and those were rewritten **mechanically from the live catalogue** (`0183`), never retyped — they include `submit_vendor_invoice` and `payment_chain_stages`, where a clause lost to a typo is a money bug.
>     - The two are **peers, not a hierarchy**: equal `role_rank`, so neither can invite the other, and both sit below the regional manager who supersedes them. Existing OEA `facility_manager` users were migrated to `property_manager` (audited); TFML's were untouched. Nobody's effective access moved — the two roles hold identical grants on the day of the split, by construction.
> 19. **A service request reaches the desk it belongs to, and no other (v3.5, 21 Aug 2026).** `tickets_select` had grown six OR branches, each added for a good reason and never read together. As a whole they said: everyone operational sees everything on their properties, finance sees the entire organisation, and **a landlord sees every complaint any tenant ever made about a building they own**. The last was nobody's policy — it is what four correct rules added up to.
>     - **The landlord leak (`0184`).** `current_user_property_ids()` does not filter on `relation`, so it answers for an `owner` exactly as for a `manager` — by design. The place branch was therefore unguarded for owners. ⚠️ **The fix was not to narrow the resolver**: it is referenced by 42 policy clauses and an owner legitimately reaches their own statements, units and payments through it. The scoping was right and the **consumer** was wrong, so the branch now states which roles it is for. B7's Service-requests cell for `property_owner` has always read "—"; a landlord sees only what they raised themselves, plus their payments and payment reports.
>     - **Finance stops reading the operational queue.** `tickets.read_all` had been granted to `finance_approver` since `0053`, derived from the pre-matrix policy rather than from B7. A request now becomes visible to a payment role only when **the money attached to it has climbed to them** — via `payments.ticket_id` (`0128`) and `current_user_payable_ticket_ids()`, the payment-desk twin of `current_user_property_ids()`. It stays visible after they act, because a record you cannot re-read is not evidence.
>     - **Org-wide sight is exactly `admin`, `executive`, `payment_audit_approver`** — named once in `request_read_all_roles()` (`0185`) rather than as three string literals in a migration, a seed and a test. The auditor is there because stage 2 of the chain checks an invoice *against the job card and the evidence*; an auditor who sees only what was routed to them is counter-signing, not auditing.
>     - **The default view moved; the reach did not.** An FM/PM lands on "Assigned to me". The property-scoped view stays one click away, because **`0178`'s review-before-dispatch gate requires them to see fresh, unassigned requests on their own buildings** — removing that would leave nobody able to triage. `finance_approver` remains the only role that disburses (decision 16, unchanged).
>     - 📌 `0184` corrected the two roles the direction *moved* and `verify-request-visibility.mjs` immediately caught a third — a drifted `property_owner` grant in the operator org. **A migration written against the diff rather than against the rule**; `0185` closes it as "anything not in the allowed set". Nil blast radius (the operator holds no client data), recorded because the failure mode outlives the instance.
> 20. **A location is a state, chosen from a list (v3.5, 21 Aug 2026).** `0087` seeded 25 major **cities** as locations. Cities are the wrong unit twice over: a city is not a jurisdiction (Nigerian property is titled, let and reported by state), and 25 cities is a **sample**, not a set — a manager in Gombe or Zamfara found no row and had to invent one, then "Portharcourt", "Port-Harcourt" and "PH" became three locations the sibling-name constraint cannot catch, because they are genuinely different strings. The **36 states + FCT is a closed set**, which is what makes it offerable as a dropdown, and a dropdown is what stops the three spellings. Grouped into the board's three regions by reading the mapping **off `0087`'s own city seed**, so nothing moves region (North 20 · South 8 · East 9). Seeded from a `nigeria_states` table that both the dropdown and `seed_org_hierarchy` read, so the offered list and the seeded list cannot drift. Untouched seeded cities are retired; **any city a manager actually filed work under stays**, and "somewhere else…" remains for a free-trade zone or campus that is not a state.
> 21. **A generator is serviced by the hour, not the calendar (v3.5, 21 Aug 2026).** `0121` added `maintenance_strategy` with a `usage` value and wired only `reactive` and `calendar`, deferring the third to Phase 2 telemetry. That conflated two different problems: a Shelly EM reporting itself is an **integration**; the number painted on the front of the generator is a **field**. Running hours govern plant here — a 500-hour interval is six weeks of grid instability or nine months of standby duty, and a calendar cannot tell those apart. `0187` adds the interval, the meter reading, the reading date and the meter-at-last-service, and `log_asset_running_hours` **refuses a reading below the previous one** unless the meter is declared replaced — the typo that would otherwise mark a machine permanently overdue. Deliberately **no automatic work-order raising**: the register says a machine is 40 hours from service; a person still raises the job.
> 22. **A unit row is one unit, vacancy is one rule, and it counts both ways (v3.5, 26 Aug 2026).** Decision 11 asks the register one question — is a unit vacant — and three parts of the build answered it differently or not at all.
>     - **`unit_quantity` recorded a number nothing could count.** `0198` let one row stand for twelve stalls and deliberately left `vacant_count` counting ROWS, which is true of the *boolean* and false of everything else: one occupant on that row read as 0 vacant, closed the window, and — because `leases_no_overlap` excludes on `(unit_id, daterange)` — permitted one concurrent tenancy, so eleven stalls could never be let. Occupancy is one `occupant_user_id`, a lease points at one `unit_id`, an invoice is raised per unit and `0009` shows a tenant their own unit by that column: **every consumer needs an identifiable unit**. "How many" now creates that many numbered rows (`create_units`), and `units_quantity_is_one` puts the rule in the database rather than the server action. The column stays at 1 so `total_factor`, `unit_total` and `effectiveFactor` keep multiplying by it correctly — `0198`'s own warning about changing what a published column means, honoured by not moving it.
>     - **"Vacant" had two definitions free to disagree.** The counters and the auto window asked "no occupant"; the lease picker asked "no live lease". Occupancy is *also* set by invitation acceptance, which writes no lease, and `activate_lease` skips the occupant entirely when there is no portal user — a company let. So a unit could read full to the window and free to the picker at once, in both directions. **`unit_is_vacant` is the one rule** (no occupant AND no live tenancy covering today), read by the property counters, the intake window, the lease form and the occupancy screen. That is decision 8's "one resolver, extended" for the third time.
>     - **It only ever counted down.** Nothing in the schema had ever set a lease to `expired` or `terminated` or cleared an occupant — `createLease`'s own error copy told agents to "End or terminate the existing tenancy first", naming an act that did not exist. **`end_tenancy`** is that act: it tells expiry from termination by the lease's own end date, and clears the occupant only if the unit still holds *that* tenancy's tenant.
>     - ⚠️ **Expiry does not vacate a unit.** `expire_due_leases` (daily, `0745`) flips an overdue tenancy to `expired` and touches occupancy not at all. A tenant holding over is ordinary here, and a date is not evidence they have gone — marking the flat vacant would advertise an occupied home. The lease's state is arithmetic; the unit's state is a fact about a person, and a person records it. Same line `0076` drew refusing to derive intake with no override.
>     - `0200`/`0201`, verified by `scripts/verify-unit-vacancy.mjs` (27 checks).

> 24. **A question is not a request, and an answer to our own question is not a new one (v3.5, 28 Aug 2026).** A live WhatsApp transcript (8–28 Aug) showed the classifier filing four questions as work orders and then, in consecutive messages, asking an open question and reading the answer as a fresh report. Read together they are one defect: **the router had exactly two answers on a cold message** — `new_request` or `pleasantry` — so anything that was not a greeting became a ticket, including a question *about* tickets.
>     - **The intent set was the constraint, not the prompt.** `list_requests` and `question` are now real answers. "Tell me about my raised requests" is **answered** from `sender_open_requests` — their own data read back, no judgement, nothing composed by a model — instead of becoming ticket 8E147AA6. A genuine enquiry the bot cannot answer still reaches a person (nothing is dropped) but is acknowledged **as a question**, with no priority menu: A2.4 keeps judgement with people, and a confidently wrong answer about a service charge is worse than a slower correct one.
>     - **The schema could hold one conversational state, so it held the wrong one.** `chat_conversations.awaiting` had a single legal value, `'urgency_confirmation'`. The branch that says *"tell us more about it, or describe something new"* therefore stored `null`, the router saw no outstanding question, and its standing "prefer `new_request` on doubt" split an answer sharing no vocabulary with what it answered. `awaiting` gains `describe_problem` and `disambiguate_ticket`, and **`chat_conversations.last_prompt` records what WE said** — the router had never been shown its own half of the conversation. Every reply now goes out through one `say()` helper that writes both; a branch that answers without recording is a bug, not a shortcut.
>     - **A quoted reference is the person telling us directly.** Nothing read a reference out of a message body, so "1F2DBAB0 … what's the stats now?" opened a duplicate of the ticket it named. `resolve_ticket_by_ref` beats the remembered thread, is **not** bounded by the 24-hour window, and returns closed tickets with `is_open` false so a status question gets an honest answer. ⚠️ **The reference is a hint, never an authority** — ownership is still `channel_sender_ref`, exactly as `0075` decided, so knowing a reference buys nothing.
>     - **Deterministic guards in front of the model**, for the messages that provably are nothing: "this is a test", "ok", a bare 1–4 with no question outstanding. These never reach a model, so they cannot be got wrong on the cheapest possible input. The four-option priority menu now prints only when the guess is worth checking (unsure, or self-escalated) — printing it every time is what trained people to send bare digits.
>     - **The model moved** from `claude-sonnet-4-6` to `claude-opus-5` at `effort: low`, and is now **resolved rather than declared** — a chain (`opus-5` → `sonnet-5` → `haiku-4-5`), then Models-API discovery with each candidate actually *called*, cached per process. `ANTHROPIC_MODEL` pins one and is tried alone; `ANTHROPIC_EFFORT` overrides the effort. The choice is evidence, not taste: `scripts/measure-router-accuracy.mjs` replays 20 real transcript decisions, five runs per model — **Opus 5 98% · Sonnet 5 92% · Haiku 4.5 95%, and all three perfect on the checks that must never move**. The safety line does not depend on the model; the deterministic guards hold it. What separates them is the *continuation* cases — is this about the open request, or a new one — which is precisely the defect this change exists to fix, so the cheaper model is the fallback and not the default. ⚠️ Three runs said Sonnet was within noise of Opus and five runs said otherwise: a twenty-case sample cannot tell a two-point gap from a six-point one, so the harness header now requires five.
>     - 📌 **`claude-haiku-4-5` returns HTTP 400 for `output_config.effort`.** Sent unconditionally, that meant `ANTHROPIC_MODEL=claude-haiku-4-5` did not make intake cheaper — it disabled the primary provider outright and ran every message through Gemini, with nothing anywhere reporting a vendor change. An unsupported **parameter** now retries the same model without it; only an unsupported **model** advances the chain. A 429 or 529 advances neither, being account- and API-wide, where walking the chain would multiply one outage by three. **A cheaper model is not a config change to make on trust — run the harness.**
>     - ⚠️ **The direction of the safe failure is unchanged.** Every change here narrows what counts as a request, and the failure mode of narrowing too far is a person reporting a leak and being told "noted" — silent, and far worse than a duplicate. `verify-conversational-intelligence.mjs` §G exists solely to hold that line, and both providers being unreachable still falls back to opening a request.
>     - `0210`/`0214`, verified by `scripts/verify-conversational-intelligence.mjs` (33 checks, every one of them a message a real person actually sent).
>     - 📌 **`0210` reproduced `revoke all … from public` for the third time in this repo**, four files after `0209` was written specifically to record it, by an author who had just read `0204` and `0209`. Five SECURITY DEFINER functions taking a caller-supplied org id and sender ref — one of them a write — shipped callable by `anon`. Prose in a migration header does not prevent the next occurrence; **the suite asserting it against a live anon client caught it within minutes**. New service-role functions now ship with `0204`'s `information_schema.routine_privileges` guard in their own migration, so a wrong revoke fails the migration instead of shipping. Note also that `create or replace` **re-applies** Supabase's default grants — `remember_conversation` was correctly closed by `0114` and silently reopened by being replaced.

> 23. **OEA's outbound flow is audit → MP → payment approver, and the administrator approves no money at all (v3.6, 28 Aug 2026 board).** Five changes, one theme: the people who *originate* work and the people who *administer* the organisation are both out of the money chain, and the desk that releases funds is named for what it does.
>     - **`finance_approver` is labelled "Payment Officer".** A **label** change only — the enum identifier is named in 123 files including `enforce_payment_transition`, `submit_vendor_invoice`, `assert_may_disburse` and `oversight_roles()`, and renaming it is a mechanical rewrite of every money-path body for a display change, which is exactly where `0183`'s lesson says a clause gets lost. What the board renamed is what a person reads.
>     - **The OEA chain is `payment_audit_approver` → `executive` → `payment_approver`** (`0211`), with the FM/PM job sign-off as the **precondition that commences it** rather than its first rung — which is what `0189` already said stage 1 was in everything but position. TFML/`direct` keep the standard ladder. Both are still exactly three stages, so `stage_order between 1 and 3`, the one-live-row-per-stage index (`0175`) and every "% of 3 stages" message needed no re-reasoning.
>     - **The MP approves *every* outbound payment**, at every amount — amending decision 9's "above the threshold". The tier check moves entirely onto stage 3. Consequently an OEA org needs a `payment_approver` whose tier covers its largest payment: the executive cannot also clear stage 3 (one human, one stage), and that is **a config gap the org closes by appointing one**, not a code exception.
>     - ⚠️ **The chain is per-organisation for the first time, and that made `delivery_brand` a control.** It was in the `authenticated` UPDATE column allowlist (`0083c`) — harmless while it chose a colour palette, an escalation the moment it chooses an approval ladder, because an administrator could have moved their own org to TFML and walked back into stage 3. It leaves the allowlist in `0211`. Decision 7 is kept, not bent: the ladder is still hardwired and configurable by nobody; what varies is a field set once at provisioning and writable by no one.
>     - **The administrator is out of money approval entirely**, on **both** ladders — amending decision 16's "an administrator approves within the threshold". `effective_approval_tier()` no longer returns a tier for them. A rule that held on one brand and not the other is the drift `0185` was written about. They still configure the org; the ladder's amounts have been operator-governed since `0149` regardless. The `remitted` transition is likewise narrowed to the payment officer alone, which `assert_may_disburse` (`0142`) had enforced since decision 16 while `enforce_payment_transition` still allowed an admin — two gates on one act, disagreeing.
>     - **The auditor sees every detail** — payment requests, invoices and attachments. Already true via `request_read_all_roles()` (`0185`), `payment_chain_roles()` (`0157`) and `0140`'s "readable by whoever can see the payment"; `verify-request-visibility` proves it rather than a migration re-granting it.
>     - **An FM sees the requests they raised**, on a new "Raised by me" view, and is reached on every channel they registered. `tickets_select` has always returned them via `sender_id` — no view asked, because the only personal view filtered on `assigned_to_user_id` and a raiser is not an assignee. Separately, the **portal** intake path wrote only the in-app bell while the chat path had used the full B8 cascade since it was written: an identical request reached an FM's phone on WhatsApp and reached only a badge from the web form.
>     - **Why the payment officer saw nothing in the demo (`0212`).** Two causes, and the second is the one that bit: `current_user_payable_ticket_ids()` (`0184`) **only ever looked at `payments`**, so an FM/PM's own request for money — an **ops requisition** (`0170`) — named a service request no payment role could open, even though the requisition itself was visible. And finance was gated behind the **whole** chain (`chain_cleared_before(…, 4)`), so the job appeared the instant before they released the money and never earlier. `0184` was written three payables ago, when a payable meant a vendor invoice. Both fixed; `tickets.read_all` is **not** given back.
>     - **A request unassigned for 24 hours reaches the administrator**, who may then dispatch it. Built **without** undoing `0178`: granting them `tickets.assign_without_review` would be permanent, org-wide and would let them dispatch a request ninety seconds old. The exception is per-ticket, time-bounded, **computed from `created_at` by the trigger itself** — so if the cron never runs an administrator can still rescue the request and only the nudge is lost — and it stamps them as the reviewer of record with an audit entry. 24 hours is **hardwired**: it is not a cadence but the width of an exception to a separation-of-duties control, and an org that could set it to zero would have turned `0178` off through the settings form.
>     - **The vendor KYC pack could not be completed, and the cause was a path (`0213`/`0215`).** The client wrote `<vendor_id>/…` while `0164`'s policy requires the org id as the first segment, so **every attach in the product failed RLS**, the pack never reached complete, and "Send for review" stayed disabled with nothing on screen saying why. `<org>/<vendor>/<doc>` is the convention `accept_vendor_introduction` already wrote for its copies — the one path a human used was the only one out of step. Three size limits (15 MB bucket / 5 MB client / the board's 2 MB in neither) are now one, HEIC is offered by neither layer, and the rules are stated **before** the file picker. A **second defect** surfaced while proving the first was fixed: a vendor holds no UPDATE policy on `vendor_documents`, so `recordDocument`'s supersede matched nothing and **returned no error** — "Replace" left two live rows. `supersede_vendor_document` (`0215`) is the narrow fix; giving vendors an UPDATE policy would hand the subject of a verification the keys to their own evidence.
>     - ⚠️ **And the pack could never be completed at all, by anyone (`0216`).** `vendor_registration_missing()` has required a **compliance declaration** since `0164` and **nothing in the product ever wrote one** — so a vendor who filled every field and attached every document was still told "still outstanding: the compliance declaration", with no control on any screen able to satisfy it. That is the demo's actual dead end, underneath the upload bug. Two more found in the same table while fixing it: a vendor could **save their details once and never correct them** (an UPDATE *policy* with no UPDATE *grant* — Postgres needs both, and the client's `upsert` hid it because the failing branch is the one that never runs on a first save), and — worse — **a vendor could file their own registration as `approved`**, because `vendor_registrations_insert` constrained the row's org but never its `status`. `review_vendor_registration` then refuses to touch an "already approved" pack, so the vendor leaves the review queue having been reviewed by nobody: decision 10's rubber stamp, reached from the subject's side. No money moves on it (decision 17 still gates no payment), but it forges the record of a human decision. `authenticated` now holds **SELECT and nothing else** on `vendor_registrations`; `save_vendor_registration` is the one write path and never takes `status` from a caller.
>     - 📌 **The suite watched one door and not the one beside it.** `verify-vendor-self-service` D6 already asserted "a vendor cannot PATCH their own pack to approved" — and passed, for a reason unrelated to intent: there was no UPDATE grant, so nothing a vendor PATCHed ever landed. INSERT was granted and untested. D6b now attempts the insert.
>     - 📌 **The pattern behind three of these:** a write that silently does nothing. A storage path RLS declines, an UPDATE a policy declines to match, an org-wide grant nobody re-read. None raised an error, and `verify-vendor-self-service` passed on the day the demo could not upload a single file — because **every fixture in it wrote through the service role**. The suite proved the policy worked and never once sat in the vendor's seat. Verified now by `verify-oea-payment-chain.mjs` (27 checks), `verify-unassigned-escalation.mjs` (13), and new section G of `verify-vendor-self-service.mjs`.

> 24. **The chain sees what it is approving, the raiser sees what they raised, and the applicant asked for more can answer (v3.6, 28 Aug 2026).** Four reports from the demo, four consumers written for one case and never re-read when a second arrived.
>     - **The auditor could not open the invoice (`0217`).** `raise_ops_requisition` has accepted an attachment since `0170` and the FM/PM form uploads it to the same `invoice-attachments` bucket a vendor invoice scan uses — but `0140`'s read policy on that bucket joins **`payments` and nothing else**, so a requisition's invoice was readable by **nobody**. Measured on the real demo requisition (OEA, ₦632,200, 94 KB genuinely present): all four chain roles saw the requisition, its lines and the job card, and none could open the invoice. Storage answers a hidden object as "Object not found", so it looked like a missing file rather than a refusal. 📌 **Third instance of one pattern** — `0171` taught the chain about `ops_requisition`, `0212` found `current_user_payable_ticket_ids()` still looking only at `payments`, and this is the same omission in a storage policy nobody thought of as a payable consumer. The detail screen also *selected* `invoice_attachment_path` and never rendered it; it now shows the invoice, the job card it was raised against, and who raised it when.
>     - **An FM could not find the work they raised (`0218`).** `raise_work_order` inserted `sender_id` as **NULL**, explicitly — so work raised through *Raise Work* belonged to nobody, and decision 23's "Raised by me" view was correct and permanently empty for the one path that fills it. `0120` chose NULL because planned work "has no reporter", which is true of a *tenant* and false of a *raiser*; the portal's own New Request has always stamped the submitter. ⚠️ The consequence had to move with it: `isTenant` on the ticket page was `sender_id === me`, which already handed an FM who used New Request the **tenant's satisfaction form** and filed their rating as `source = 'tenant'`. It now means "the reporter who holds no management authority", and **`0220`** says the same thing in the database — a tenant-source rating requires the caller to *be* a tenant, so an FM cannot score the satisfaction half on a contractor they hired, on a composite that gates payment.
>     - ⚠️ **The applicant asked for more information could never answer (`0219`).** `record_application_info_request` reopens an application as `status = 'info_requested'`; `resume_application` matched `status = 'draft'` **and nothing else**. Proven with the real OEA applicant's own unexpired token: **0 rows** — they see *"This link no longer works"*, and there is no other way in. `save_application_draft` was draft-only too, so an answer could not have been kept even if they got in. 📌 **`0082d` fixed the two functions DOWNSTREAM of the door** — attachment upload and submit — and never asked which *other* functions match a resume token on `status = 'draft'`. Asking the catalogue instead of guessing found both. And `submit_tenant_application` had **no notification at all**, on a first submission or a resubmission, where the vendor twin has had one since `0164` — so the reviewer asked, the applicant answered, and nobody was told. The reviewer's own words now travel with the resumed form, which until now existed only in the email.
>     - ⚠️ **And the fixes did not reach the people who reported them (`0221`/`0222`).** Three of the four above were correct and still invisible on the screen that matters.
>       **The chain could not name anyone.** `users_select` gates on `oversight_roles_with_fm()` (`0072a`), written before `0151` created `payment_audit_approver` and `payment_approver` — so those two read exactly **one** row of `users`, their own, out of 112, and every completed stage rendered *"Approved by someone no longer listed"* to the auditor whose stage exists to check it. Nobody had left. 📌 This is `0157` one table over, and that migration's own header names the fault — *"0151 … gave them authority over those tables without giving them sight of them"* — having fixed `payments` and `remittances` and not `users`. It is reached by an **embed** rather than a query anyone wrote, so it failed as a wrong sentence rather than an empty screen, which is harder to notice and worse to leave.
>       **The queue said "invoice attached" and offered no way to open it.** `0217` made the evidence readable and the detail page renders it; the Approvals list gained only those two words. The board's rule is that every touch point sees the detail **at their desk**, and their desk is the queue — so what is being approved (reference, raiser, date, the job card, the lines, and the invoice itself) now renders on the card, for the row waiting on them and the rows waiting on someone else alike. Two extra queries and one signing call for the whole page, not one per row.
>       **A fix that only applies going forward is not a fix for the person who reported it.** `0218` gave `raise_work_order` a sender and did not give one to the work already raised: measured live, **every** FM and PM in both brands had `raised = 0`, including the FM who raised the generator job the demo's own requisition was drawn against. `0221` recovers them from `reviewed_by`, which that function has stamped with `auth.uid()` since `0178` — bounded to `channel = 'portal'` so a triaged WhatsApp complaint is never re-attributed to the FM who triaged it, and leaving the 20 rows older than `0178` unattributed rather than guessed at.
>       And **"Raised by me" is now offered to the administrator**, who could raise a request and had no view listing it.
>     - ⚠️ **An FM/PM's dashboard landed on an empty view while their work sat one click away.** The default scope was `mine` — `assigned_to_user_id = me` — and an FM/PM is not primarily *dispatched* work, they *dispatch* it. Reproduced exactly on the live portal's data: the landing view showed **0**, `?view=properties` showed **3**, including the generator job their own requisition was raised against. That is the whole of "they can only see them on their notifications". The landing view is now **`desk`** — assigned to me **OR** raised by me **OR** on a property I manage — resolved server-side with `current_user_property_ids()`; the three narrower views remain. Still a narrowing of what `tickets_select` released, never a widening.
>     - **And nothing arrived without a refresh.** The realtime filter was `scope === "mine" && assigned_to_user_id === me`. A NEW request is unassigned by definition, so it could never satisfy that — nothing ever appeared live on the view an FM/PM actually sits on. The filter now mirrors the `desk` query, and the property ids come from the server: the browser is told which places are yours, it never decides.
>     - 📌 **Two copies of one list will disagree, and did.** `RequestStats` was rendered from the server's array while `TicketList` kept its own `useState` plus its own subscription — so a request arriving over the socket moved the list and not the tiles. That is the reported symptom in its purest form ("OPEN REQUESTS 1" above "All 0"). `RequestsBoard` now owns one array and one subscription and both consumers read it, so they cannot drift. **A screen that contradicts itself is not a display bug; it is two sources of truth.**
>     - **A vendor's My Work and an operative's My Jobs re-fetch instead of splicing** (`LiveRefresh`). Those pages join tickets to payments, evaluation readiness and invoice state, and a socket message carries only the ticket — splicing the row would show a new job beside stats computed before it existed, which is the same fault from the other direction. Asking the server again keeps one source of truth for the whole page, and grants nothing: it re-runs the same RLS-scoped queries.
>     - 📌 **The shape common to all four:** a consumer written when there was one case, still correct for that case, silently wrong for the second. A payable type, a sender that is not a tenant, an application status that is not `draft`. **Adding the second case is not finished when the writer accepts it — it is finished when every reader of the first has been re-read.** Verified by new sections in `verify-application-review` (the door, the reason shown, the edit saved, the reviewer told), `verify-fm-journey` (raised-by-them, and the rating guard that replaces the null), and `verify-vendor-self-service`.

> 25. **A statement exists per tenant, per property and per landlord, it says how it was split, and what a landlord is charged is not shown to their tenant (v3.6, 30 Aug 2026).** The reporting question — *"can a tenant, a landlord or an auditor be handed a clean account of their own money?"* — had four different answers, three of them "no", and answering it surfaced an access defect underneath.
>     - **The drawer card opened nothing (`0225`/`0226`).** `LeaseStats` linked every card to `/dashboard/leases/<lease_id>` and **that route had never been built** — the one stat drawer in the dashboard whose target did not exist, against seven that do. Clicking any tenancy in any lettings tile produced the app's 404. The tenancy page now built to close it is also the **per-tenant statement**: term, rent, deposit, every demand and every service charge on the unit, receipts joined **through the charge** rather than the unit (the unit column is not populated on every path, so the join through it can silently return nothing).
>     - **Apportionment states its method, and a person may state it (`0227`).** The split was hardwired pro-rata by an `apportionment_factor` typed per unit. `sc_budgets.apportion_method` ∈ `area | equal | manual`, defaulting to `area` so every existing budget and every suite is byte-identical; `manual` reads `sc_budget_shares` and **generation is refused unless the stated shares reconcile to the budget total**, which is the invariant the automatic path guarantees for free and a hand-entered one does not.
>     - **A property can account for itself (`0228`).** `landlord_statement` answered what one OWNER is owed, `org_profit_and_loss` what the ORGANISATION earned, `my_service_charges` what one TENANT was billed — and nothing answered *"show me this building"*, which is the question a landlord asks on the phone and an auditor asks in writing. Scoped through `current_user_property_ids()` and `oversight_roles()` — decision 8's one resolver, extended, never a second.
>     - **And the landlord's own copy was a different account of the same building (`0230`).** `landlord_statement` carried no service-charge column at all, so the owner's screen and their property's statement disagreed: measured live, Parkview Terraces showed ₦71,000,000 billed and ₦18,000,000 collected to a manager and **nothing whatever to its landlord**. ⚠️ Rent and service charge are reported side by side and **never added** — rent is collected FOR the owner and remitted net of fees, service charge INTO a fund the building spends; a combined total is the 0103 cross-currency mistake with a friendlier label. The SC join goes through `sc_budgets.property_id`, the same rule `property_statement` uses, so two reports about one building cannot disagree again.
>     - ⚠️ **The fee split was readable by the tenant, and the recorded note said it was not (`0229`).** `0226` gated the fee columns on the page and recorded the exposure underneath as *"not reachable on any screen"*. Measured as the live tenant: 24 `rent_charges` rows carrying `management_fee_amount` and `landlord_net_amount`, and — the one that matters — **18 `rent_roll` rows, 15 with a non-zero split**. `rent_roll` is `security_invoker` and publishes the sums as columns named `management_fees` and `landlord_net`, and `/dashboard/leases` guards the module and the write buttons but **never the read** — so a tenant who typed that URL was rendered their own tenancy under a column headed **"Landlord net"**. 📌 **A claim that something is unreachable is a measurement, not a reading.** The probe took a minute; the note had stood on an inspection of the page it was written on.
>     - **The fix was neither column privileges nor a new view**, which is what the note had proposed. Column privileges are per-ROLE and every signed-in person is `authenticated`, so revoking the fee columns takes them from the payment officer and the landlord too. And the view already existed: `my_rent_charges()` (`0110`) is definer-scoped to the caller's own tenancies and has never returned a fee column. The tenant had a resolver **and** a second way in; `0229` deletes the second — decision 8's rule applied by removing the duplicate rather than adding a third thing. The tenancy page reads `my_rent_charges()` for a tenant, so the fee columns are no longer merely un-rendered, they are **never sent**.
>     - ⚠️ **`rent_roll` had to state its audience too, and leaving that out would have been worse than doing nothing.** With only the policy narrowed, a tenant still matched `leases_select` and still got their row — with every money column silently `coalesce`d to **0** on a tenancy billed ₦6,000,000, because the lateral could no longer read the charges. A refusal is honest; **a zero that means "you may not see this" is indistinguishable from one that means "nothing was billed"**.
>     - 📌 **A period picker that navigates somewhere else is not a period picker.** `PeriodPicker` hardcoded `/dashboard/ledger/reports` and was reused on the property statement, so pressing **Apply** threw the two audiences that page exists for — the property's manager and its landlord — onto the ledger, which is gated to admin, finance and the executive, i.e. onto *"Finance access required"*. It now takes a `basePath`. The landlord portfolio and the tenant's own statement also gained the **print masthead** they had never had: a statement nobody can put on paper is not a statement, and these are the pages a landlord, a client or an auditor is handed.
>     - Verified by `verify-tenancy-statement.mjs` (31), `verify-property-statement.mjs` (29 incl. the landlord's own copy, both screens' rules, and the anon revoke a drop-and-create re-opens), and `verify-apportionment.mjs` (24) — plus the fifteen money and access suites re-run unchanged.

> 26. **A regional manager supersedes the desk below them, and the operational desk sees the budget on a building it holds (v3.6, 30 Aug 2026 board).** Reported from the live OEA portal as three separate complaints; they are one theme and one of them is not a permission at all.
>     - **The read was never the problem.** "Service Charges is missing for a property manager" reads like a missing capability and is not one. `sc_budgets_select` has been *oversight, or `property_id in (select current_user_property_ids())`* since `0055`, and `service_charges_select` reaches the same rows through `budget_id` — so an FM, a PM and a regional manager could **always** read the budgets on their own buildings. What hid the module was `seesServiceCharges` in the dashboard layout, derived as `can('sc.read_all') || can('sc.manage')`, neither of which any operational role holds. ⚠️ **The fix is the nav, and granting `sc.read_all` would have been wrong twice over**: its own description is *"read every service charge, not only their own"* — it is the ORG-WIDE capability, which decision 9 explicitly denies this role. Handing it out to make a menu item appear trades a property-scoped read the policies already do correctly for an unscoped one. *A capability that makes a screen appear is not thereby the capability that screen needs.*
>     - **`hierarchy.write` was granted to `admin` and nobody else**, against decision 8's own words — *"the FM/PM builds the tree while filing a property … a picker that can only select is a dead end for the first property in a new city."* It is named in no branch of `seed_b7_permissions` and fell to its `else false`. Recorded 29 July 2026, never implemented, and nothing failed loudly: the inline creation simply was not offered. It now goes to `fm_roles()`.
>     - **The regional manager gains `sc.manage` and `leases.write`** — the B7 amendment, minuted rather than assumed, since B7's Regional Manager row read "—" for SC & financials and decision 9 said "nothing financial". `role_rank` has ranked them **60** against the FM/PM's **50** since `0183`; a role that supersedes the FM/PM over a wider place cannot be unable to administer the service charge and the tenancies on the buildings it holds. This makes the authority match the rank. **`sc.read_all` stays denied** — their reach is the place, not a blanket.
>     - ⚠️ **It is bounded by place, not by trust, and that clause had to be written first.** `sc_budgets_insert/update/delete` tested `has_permission('sc.manage')` and the org and **nothing about which property** — harmless while the only holder was the payment officer, whose remit *is* the organisation, and not harmless the moment a regional manager holds it: without it, a manager in the South could raise a budget against a building in Kano. All three gain the clause `leases_write` has carried since `0090`, and `service_charges_insert/update` move from a hardcoded `('admin','finance_approver')` pair to the same capability-plus-place test. Oversight is unchanged **by construction**, not by inspection: admin, finance and the executive are all in `oversight_roles()`.
>     - **People stopped refusing them.** `0183`'s invitation and vendor-decision functions have always admitted `regional_manager`, and `people.invite` is theirs in the baseline — only `people/layout.tsx` said no, gating on `["admin", ...FM_PM]`, while the nav went on offering the link. Decision 9's *"plus inviting operational staff"* was unreachable through the product for as long as it has existed. The nav and the page disagreed and **the page was the wrong one**.
>     - 📌 **`oea.fm@` was never a broken login.** It is seeded as a `facility_manager` and then converted to `property_manager` by `0183` along with every other OEA FM — the split doing exactly what decision 18 says, since OEA's existing "facilities managers" were its property managers. The row moved; the email string did not, and `seed-brand-demo-content.mjs` went on printing it under an "fm" heading. **A fixture whose name contradicts its row will be read as a bug in the product.** OEA's real facilities manager is `oea.fmgr@`; TFML's `tfml.fm@` was never converted, which is why the two brands differ.
>     - 📌 **The migration was first drafted against `0205`'s copy of `seed_b7_permissions` and was four changes stale** — `0233` had added `records.export then false` and `0225` had moved `applications.approve` from the executive to the regional manager. Retyping it would have handed an administrator `records.export` and taken tenancy approval back off the region, silently, in a migration whose subject is something else entirely. Rebuilt mechanically from `pg_get_functiondef` with exactly two lines inserted. **`0183`'s rule is not advice: the live catalogue is the source, never the migration that last wrote it.**
>     - 📌 Recorded, not fixed: `service_charges` has **no DELETE policy** and a `no_hard_delete` trigger, so `generateInvoices`'s delete-then-reinsert clears nothing under RLS and would duplicate a budget's invoices on a regenerate. Guarded today only by the UI disabling the button once `status = 'invoiced'` — a control in the wrong layer, unchanged in risk by this turn, and owed its own.
>     - `0236`, verified by `scripts/verify-regional-authority.mjs` (23 checks), with apportionment, property-statement, tenancy-statement, collections, leases-and-rent, permissions, access-matrix and request-visibility re-run unchanged.

> 27. **The service-charge fund belongs to a property, and a refusal says which one (v3.7, 5 Sept 2026).** The payment officer could not send a ₦21,000 requisition: *"account 2000 would be overpaid by 21000.00 — a counterparty cannot be paid more than is owed to them."* Measured on staging, that sentence was three things at once and only one of them true. It was **right to refuse** — `assert_funds_available` (`0027`) exists to stop service-charge money being spent before it is collected, and OEA's 2000 held ₦0.00 that day; the ₦405,927.73 on it now arrived on 4 Sept, after the refusal. It named **a concept that does not apply** — 2000 is `service_charge_fund`, a pooled source with no counterparty at all, and the sentence belongs to 2200/2400, which genuinely are one row per payee; it printed for the wrong kind of account because both are `class = 'liability'`. And it **could not name which building was short**, because there was only ever one row to be short.
>     - ⚠️ That last point is not a message problem. Decision 2 requires "a segregated client-funds account, an in-app segregated ledger", and one pooled balance is the opposite: it lets one building's shortfall block another's fully-collected payment, and — the direction that matters — lets one building's vendor be paid out of another building's tenants' money with nothing in the schema noticing. `0027`'s own comment already called 2000 "SC collected **for a property's** budget". The column to do it with (`ledger_accounts.property_id`) has existed since then and was **never once populated**: 0 of 36 rows across 4 orgs.
>     - **`property_scoped_ledger_purposes()` is the one place** that says which purposes are held per property — decision 8's rule, so adding `landlord_payable` later is one line. Fund rows become sub-accounts (`2000.001`, `2000.002`) under the control account, which is what an accountant expects and which deliberately does not match `canonical_ledger_account`'s numeric-code preference, so the three-argument (org-level) lookup still resolves plain 2000 exactly as before.
>     - **History is reallocated, not rewritten.** Every posting stays where it was made; the balance moves by a dated journal entry, which is what an accountant does when a control account is broken out. Only what can be *attributed* moves — a collection knows its invoice, the invoice its budget, the budget its property. Measured: OEA's ₦405,927.73 lands on Osborne Tower's own fund, and the ₦21,000 requisition that failed now reads `sufficient: true`.
>     - **A payable that names no property is not refused.** 45 of 45 POC payments and 22 of 22 POC requisitions carry no ticket and therefore no property; refusing them would stop real money on a data gap the payer did not create. They draw on the org-level row, which is now *named* "unattributed" rather than being the only fund there is.
>     - **`payable_funding_state()` asks before the send**, so the queue shows "Osborne Tower's fund holds ₦405,927.73 — enough for this" or the exact shortfall, instead of a COMMIT-time exception being a person's first warning. `0247`.
> 28. **A chain may be one stage, and only the operator says so (v3.7, 5 Sept 2026).** Asked as "remove the tiered approval chain, or make it optional from the org admin setting." The second half is refused on the board's own record: decision 7 lists payment approval among the controls that "stay hardwired and never appear as toggles", and decision 23 took `delivery_brand` out of the `authenticated` allowlist three weeks ago *the moment it began choosing an approval ladder*. Handing the ladder itself to an org admin is that same escalation with the middle step removed. So `orgs.approval_chain_shape` ∈ `standard | oea | single_stage`, set **only** by `operator_set_approval_chain()` behind `caller_is_operator_admin()`, absent from every org-writable column grant. **Nothing changes for any org until an operator acts**: NULL means "derive from the brand, as before", so the migration is behaviour-preserving by construction rather than by inspection. `single_stage` is one rung and that rung is the **payment approval** — collapsing a ladder has to keep the stage that authorises money leaving, and keeping stage 1 would have made an FM's sign-off the whole authorisation. 📌 `apply_chain_outcome_to_payment` hardcoded `stage_order = 3` twice; it was the one chain function not resolving the stage list dynamically. `0248`.
> 29. **The property manager administers the money on their own buildings — and the facilities manager does not (v3.7, 5 Sept 2026).** Decision 26 gave `sc.manage` and `leases.write` to the regional manager and stopped. This gives both to the **property** manager. ⚠️ This is the first deliberate divergence between the decision-18 peers: `0183` split them saying they "hold identical grants on the day of the split, by construction", and that day has passed — TFML's FM maintains plant, OEA's PM lets and administers tenancies, and it is the letting side that raises a service charge and writes a lease. Confirmed with the board before writing. Every write it unlocks is bounded to a property they hold by the clause decision 26 already put on `sc_budgets_*` and `service_charges_*`; this grants the capability, never the reach.
>     - 📌 **The writer was allowed and the reader denied.** `payment_intents_insert` has admitted FM/PM/RM since it was written, while `payment_intents_select` gates on `oversight_roles()` — so a manager could raise a collection and then not see the row they had just created, or whether it was ever paid. Same failure mode as `0216`'s vendor UPDATE-policy-without-a-grant: nothing errors, the row simply is not there.
>     - **`property_finance_roles()` names the two desks once.** ⚠️ Required by decision 19's correction: `current_user_property_ids()` does not filter on `relation`, so a bare place clause on a *ledger* policy would hand every landlord their building's vendor payables and this org's fee income — the exact leak `0184` had to go back and close on `tickets_select`. The branch states which roles it is for. Ledger **posting**, bank configuration and disbursement are untouched. `0249`.
> 30. **A refusal returns the payment to the desk before it (v3.7, 5 Sept 2026).** `apply_chain_outcome_to_payment` set the whole payable to `rejected` on a refusal at **any** stage, so a typo caught at the Managing Partner's desk killed the requisition outright — and `0170`'s own header says an ops requisition then had no way back at all ("no path that lets a raised requisition be edited after the fact rather than rejected and re-raised"). A vendor payment's only route back restarted the *entire* B4 gate, discarding stages 1 and 2's correct work along with the mistake. A refusal is now two acts: **`rejected`** (terminal, unchanged) and **`returned`** — back one rung, retiring the approval at stage N-1 so that desk must look again; at stage 1 there is no rung below, so it goes to the raiser and the payable reads `returned_for_correction`.
>     - ⚠️ **It re-climbs from the rung it was returned to, not from the bottom.** Stage 2 sending something back does not erase stage 1's approval from the record, but it does void it — a signature given on the figures as they were is not a signature on the figures as corrected. Same reasoning `enforce_approval_rules` already applies when an amount moves, which is why the return supersedes rather than deletes.
>     - **The tier check now applies to an approval only.** Refusing or returning something you are not senior enough to *approve* is the cheapest possible outcome, and requiring a tier to say "this is wrong" left a junior approver able neither to clear a payment nor to send it back. `apply_chain_outcome_to_payment` already short-circuited on a refusal while `enforce_approval_rules` still demanded the tier first — the two disagreed; they now agree.
>     - 📌 **The one write path had to admit the third decision.** Every rule could have been in place, the UI could have offered the button, and `record_payment_approval` — the only way in, by design — would still have answered "a stage is either approved or rejected". **And the trail is now rendered whole**: `getChainState` stopped excluding superseded rows at the query, so "even though an approval has been given the approvers should still be able to view the movement" is answered by showing every round, retired ones struck through. `0250a`/`0250b`.
> 31. **A property is filed with at least one unit, in one transaction (v3.7, 5 Sept 2026).** Units were labelled "(optional)" and the submit guard was the property name alone. ⚠️ Worse, and invisible: the two writes were **never atomic** — `saveProperty` then `saveUnit`, with a failure branch whose own copy read *"Property added, but its units were not."* So the state this forbids was not merely allowed, it was **produced** by any failure of the second call, and the person saw a partial-success message and moved on. `create_property_with_units` is one function body and therefore one transaction. **Not a table constraint**: 2 of 17 live properties have no units, and forcing a repair as a side effect of a migration about creation would mean inventing unit data for two real buildings. They are grandfathered and reachable instead — the tenancy form's "add a unit to the property", which had been **plain text rather than a control** since it was written, is now the control it claimed to be. 📌 `create_property` takes four arguments and `property_type` is not one of them, so the create path has silently dropped it since `0237`. `0252`.
> 32. **A decided tenancy application does not disappear (v3.7, 5 Sept 2026).** The queue and its badge both filtered to `submitted, under_review, info_requested`, so the instant a reviewer approved or rejected an application it left the page, the count and the product, with no list anywhere still holding it. Decision 10's whole basis is that the reviewer's own recorded reason is **contestable**, which requires it to be readable. Open / Approved / Rejected / All are now views of one page; the badge still counts open work only. Sorting carries the view, because a control that throws the reader back to a different list is the fault decision 25 recorded about the period picker.
> 33. **The receipt is sent, the collection says whose money it is, and the header stays put (v3.7, 5 Sept 2026).** Four screen-level reports, one theme: the product knew the answer and never showed it.
>     - **The receipt was never sent.** Traced: no `sendEmail` exists on the collections webhook and never has. The only receipt is a PDF rendered on demand behind a signed-in session, linked from inside the dashboard — so a tenant who paid a link and holds no portal account had no route to one at all, while `lib/guides/processes.ts` told them a verified payment "issues a receipt in real time". ⚠️ And once a send was added, half the collections still had nowhere to go: `raisePaymentRequest` resolves an address carefully, hands it to the gateway and **discards it** — `payment_intents` had no email column, so for the "Unassigned" rows the Collections screen raises against, the address the person typed was the only one that ever existed (`0253`). The send sits after the ledger post and swallows every failure: a mail provider being down must never turn a settled collection into a webhook the gateway retries.
>     - **Collections now states whose money it is.** It mixed tenant billing (service charge, rent, deposit) with the ad-hoc/FX card raised against a vendor, a landlord or a client — the page's own copy admitted this while the table showed them together. Split by who is being asked, with a CSV of the list actually on screen.
>     - **Sort was applied after the cap, so "Oldest first" could not return the oldest row.** Each approvals query was capped at 100 *newest* and then re-sorted in the browser; the longest-waiting payment, the one the option exists to find, was the one it could never show. Ordering moved into the query, date-range search with it, and the cap now says so when it bites.
>     - **The table header is sticky**, on the shared primitive rather than per page — the alternative was three classes pasted into a dozen files and missing from the thirteenth.
> 34. **The audit trail records the chain and the requisition (v3.7, 5 Sept 2026).** Asked as "is this what the audit log will actually be like, stating the actual actions that took place by whom, at what time?" For most of the platform, yes: `audit_log` has covered ~60 tables through `log_audit()` since `0005`, is genuinely immutable (`prevent_audit_mutation`), and holds a real before/after row diff. But grepping every migration for `on payment_approvals` returns three triggers, **none of them `log_audit`**, and `on ops_requisitions` returns none at all. So every individual approval decision — stage 1's sign-off, stage 2's audit review, stage 3's final approval and every refusal — was recorded only in `payment_approvals` and appeared **nowhere** on `/dashboard/audit`; and an ops requisition's entire life left no trace there whatsoever. "Who approved this, and when" is the substance of the question and was the part missing. `0251`.
>     - ⚠️ **The standard is ISO 41001 §7.5, not ISO/TR 41016:2024 (board, 5 Sept 2026).** The request named ISO/TR 41016:2024 as the standard these records must be kept "in tandem with". Checked rather than accepted: that document is *Facility management — Overview of available technologies* — a catalogue of FM technology categories. It does not say what an audit trail must capture, and citing it as the basis of a record-keeping control would have been a compliance claim with nothing behind it. **The board has confirmed the intended reference is ISO 41001**, whose §7.5 *Documented information* is what actually governs the creation, control, retention and protection-from-alteration of records like these — and which this schema was already built against: `audit_log` is append-only by trigger (`prevent_audit_mutation` refuses UPDATE and DELETE), attributable (`actor_id`), timestamped, and carries a real before/after row diff rather than an event label. 41016 is not cited anywhere as a control basis; where the platform's own technology choices are described it is at most descriptive. 📌 The lesson is the citation itself: a standard number in a requirement is a claim to be verified like any other, and this one would have been repeated into a compliance pack unchallenged.
> 35. **The tenancy schedule is generated, not typed (v3.7, 5 Sept 2026).** Built against `private/MANAGEMENT PORTFOLIO.xlsx`, the workbook this portfolio is actually kept in: 18 sheets, 13 of them a property ledger with a header block naming the LANDLORD, the PROPERTY ADDRESS and the DESCRIPTION, then a tenant table — *S/N · NAME OF TENANT · PHONE · TENANCY PERIOD · RENT P/A · SERVICE CHARGE · AMOUNT PAID · SIZE/SHOP NO./FLOOR · MGT FEE @ n% · OUTSTANDING · REMARK*. **Every one of those already existed in this database**, across six tables and reachable from no single screen. That is why the spreadsheet survived alongside the product: not because the data was missing, but because nothing assembled it.
>     - **The fee percentage varies per property** — 5%, 7%, 7.5% and 10% all appear, one rate per landlord block. That is decision 14's org-default-plus-landlord-override, and it is read from the rate **snapshotted onto the charge**, never from the org's current setting: a later rate change must not silently rewrite a past schedule.
>     - **"Paid and remitted" vs "paid and not yet remitted"** is carried by hand in the REMARK column and is the difference between money collected and money passed to the landlord — `rent_collected` against `landlord_net`, which the ledger already knows.
>     - **Rent and service charge sit side by side and are never added**, per decision 25.
>     - ⚠️ **The audience is stated.** `security_invoker` means `leases_select` admits a **tenant** to their own row, so without the predicate a tenant reaching this view would be shown their own tenancy under a column headed "Landlord net" — exactly the exposure `0229` closed on `rent_roll`. They are refused rather than served zeroes: a zero that means "you may not see this" is indistinguishable from one that means "nothing was billed".
>     - Grouped by property, landlord or tenant; sortable, searchable and date-filterable **in the query**; printable with a masthead; downloadable as CSV **of the filtered view**, because a download button under a filtered list that hands over the whole portfolio is both useless and the wrong default for a report leaving the platform. 📌 Bulk export stays gated on `records.export`, which `0239` turned off for every role including admin four days ago — the sanctioned lever is the operator's permissions matrix, and quietly rewriting the baseline in a migration about something else would have reversed a DPA control. What *was* wrong is that `/api/records/export` refused anyone but `admin` regardless of the capability, so it could be granted to a property manager and change nothing. 📌 **The lever has since been pulled** (board, 5 Sept 2026): `records.export` is granted to `admin`, `property_manager`, `regional_manager` and `payment_approver` — the senior accounting desk (decision 23), for reporting and record generation — on **TFML and OEA**, through `set_role_permission` as a signed-in operator administrator, so `set_by` names a person. The **baseline is untouched** and still OFF for every role — this is a badged, one-click-revocable deviation, which is exactly the shape decision 7 designed. `facility_manager` is deliberately excluded (decision 29), and so is `finance_approver`: the payment officer RELEASES money, and pulling a roster of tenants out of the platform is not part of releasing money — the two are separate acts and stay separately granted. The client orgs (*Foundation POC*, *Service Charge Client*) are not enabled: turning on bulk PII export for a client organisation is that client's decision, per `0239`'s own "only when asked".
>     - 📌 **And the refusal had to stop lying.** Every capability refusal in the product read *"isn't turned on for your ORGANISATION yet"*, which is true only while nobody in the org holds it. `records.export` and `training.read` are granted **per role per org**, so the moment the administrators and property managers hold export, that sentence sends the payment officer to ask for something their organisation already has. `lib/capability-refusal.ts` asks the matrix which case it is — *some role holds it* → **"not turned on for your role yet"**; *no role holds it* → the org wording — and the five refusal sites (record export, document zip, training PDF, the schedule's download, the training screen) all read from it rather than each carrying its own sentence. Asked under the CALLER's own session: `role_permissions_select` is already `org_id = current_user_org_id()`, so nothing is disclosed that Settings → Permissions does not already show them, and no elevation was needed to tell a person why they were refused. `0254`, verified by `scripts/verify-portfolio-and-controls.mjs`.

> 36. **A tenancy is offered, then accepted, then recorded — and three of the four letters an applicant should receive did not exist (v3.7, 6 Sept 2026).** Asked as *"what email/letter is supposed to follow a successfully approved application? is it sent now?"* Tracing all four states end to end found that the answer to the second question was mostly no, and that the first had no data behind it at all.
>     - **The sequence was backwards.** `record_application_approval` (`0082`) issued a PORTAL INVITATION and emailed *"your application was approved — set up your account"*. That is an account, not an offer: it names no rent, no term, no deposit and nothing to accept. In Nigerian lettings an approval is followed by a **letter of offer** — the unit, the term, the rent, the service charge, the deposit, what is payable on acceptance, and a date by which to accept — and it is the document a tenant pays against. ⚠️ **Nothing in this schema held one of those figures at the moment of approval.** `units` carries a label and an apportionment factor and no asking rent; rent first existed when a **lease** was written, which is *after* the tenant is supposed to have agreed to it. So the product created the tenant's account before anybody had told them the price. `tenancy_offers` (`0263`) holds the terms, `issue_tenancy_offer` is its one writer, and **acceptance is what creates the invitation** — an account is the consequence of accepting, not the way of being told.
>     - **The old three-argument signature was DROPPED, not defaulted.** An optional-terms version leaves the invitation-issuing path callable, which is two ways to approve an application with different consequences and one of them the behaviour being replaced. Everything in the new body that is not about the offer — the maker-checker rule, the property scoping, the corporate two-approver count, the unit precondition — is `pg_get_functiondef` output, moved and not retyped (`0183`).
>     - **A corporate applicant's FIRST approval makes no offer.** Terms stated by one approver before the second has looked would be an offer the organisation had not finished making. The form is not even shown to them.
>     - **Lapsing is arithmetic, never a swept status.** Nothing runs a job over these rows, so a stored `lapsed` would be a lie the day after a cron failed — and worse, a stale offer would stay acceptable. The page and `accept_tenancy_offer` apply the same test, so what a person is shown and what the database permits cannot disagree. Same line decision 22 drew refusing to let `expire_due_leases` vacate a unit.
>     - **An offer can be withdrawn and re-issued**, which kills the old link by rewriting its token hash. Without it an offer with a mistyped rent is terminal — the application is already `approved`, so the approval path cannot be walked again, and the one live offer holds the unit. That is decision 30's dead end reached from the lettings side. ⚠️ Consequently **the acceptance link cannot be shown in the dashboard**: only its hash is stored, so the one copy that ever existed is in the applicant's email. A "resend" button that could not resend the same link would be worse than its absence; withdraw-and-re-issue is the honest remedy and the screen says so.
>     - **The two silent paths.** **Submission** sent nothing at all — a person hands over identity documents, bank statements and their employer's details, and their reference appeared once on the screen behind them and nowhere afterwards. **Rejection** sent nothing either, which is not merely discourteous: decision 10's whole basis is that the reviewer's recorded reason is **contestable**, and a person cannot contest a decision nobody told them about. Both now send, the rejection carrying the reviewer's own words, the right to ask for a re-read, and what the 90-day retention actually means. All four applicant letters live in one module (`lib/application-mail.ts`) because they are one voice speaking to one person across one process.
>     - **The offer letter says "reply before paying anything", on the page as well as in the email.** Advance-fee fraud against prospective tenants is ordinary in this market, and the defence is a person who knows the real figures and expects to be told them in writing first. The page is that writing.
>     - **The lease is prefilled from the accepted offer** (`/dashboard/leases/new?offer=…`), read through the caller's own session so RLS decides. The service charge is stated beside it and deliberately **not** folded into the rent (decision 25) — a lease holds rent; the service charge is raised from the property's own budget.
>     - 📌 **`revoke ... from public, anon` is not the list, and this is the FOURTH instance.** `0204` recorded it, `0209` was written to record it again, `0210` reproduced it four files later by an author who had just read both — and every one of those was about `anon`, so the reflex that formed is exactly the one that misses **`authenticated`**, which Supabase's default privileges grant on every new function. `0263` shipped four functions still callable by any signed-in user on its first run; `verify-function-grants` then found a fifth, `ensure_property_ledger_account`, over-granted the same way by **`0247`, written in this same week's work** (`0264` closes it). Prose in a header has now failed to prevent three recurrences. **The suite is the control**, because it compares the live catalogue against what each migration declared and is the only thing that can see a grant nobody wrote.
>     - 📌 **And a check that fails for the wrong reason is not a check.** The first draft of the duplicate-offer probe called `issue_tenancy_offer` through the SERVICE ROLE and "correctly" failed — with `no such application`, because that function scopes on `current_user_org_id()`, which is null for the service role. It never once exercised the rule it names, and would have passed the moment somebody loosened its regex. The lapse probe failed the same way from the other direction: backdating only `expires_on` was silently rejected by `tenancy_offers_deadline_sane`, so the row never aged. `verify-vendor-self-service`'s own recorded lesson — every fixture writing as the service role proves the policy and never sits in the user's seat — one suite over.
>     - 📌 **And the public pages had been naming the wrong thing all along.** Rendering the finished offer page found `brandName` resolved as `portal_name || name` — and OEA's `portal_name` is **"PM PORTAL"**, so every prospective tenant who has ever opened that brand's application form was told they were dealing with an organisation of that name. The dashboard has always had it right (`name` as the title, `portal_name` as the small label under it), and `sendEmail` has always resolved `email_from_name || name` and never looked at `portal_name` — so the offer letter said "Ora Egbunike & Associates" and the page it linked to said "PM PORTAL", about the same offer. On this page it is not cosmetic: the whole security value of *"pay only into an account in this name"* is that the name can be checked against a bank account, and PM PORTAL cannot. `publicOrgName()` is the one resolver both public pages now read. **A column named for a label was being used as an identity, and only rendering the page showed it.**
>     - `0263`/`0264`, verified by `scripts/verify-application-review.mjs` (72 checks), with application-submission, document-checks, function-grants, ledger, access-matrix and unit-vacancy re-run unchanged, and the offer page rendered end to end on staging data (issued and declined) before the fixture was removed.

> 37. **A rent roll arrives as a file, and the tenant of record has a name (v3.7, 6 Sept 2026).** Asked as "there should be bulk upload/import" on the tenancy schedule. That schedule IS `private/MANAGEMENT PORTFOLIO.xlsx` rendered live (decision 35), and the reason it renders **empty** for OEA is that thirteen sheets of existing tenancies have never been entered — one at a time, through a form, is not a migration path for a portfolio that is already let.
>     - ⚠️ **It surfaced a gap that had to be closed first.** `leases.tenant_user_id` is nullable and `tenancy_schedule` read the tenant's name off the joined `users` row. Right for a tenant with a portal account; wrong for everybody else — decision 22 already records that a **company let has no portal user**, and `activate_lease` skips the occupant entirely in that case. The workbook's own second column is NAME OF TENANT and it is never blank. So a company let, and every row of any import, rendered with **no tenant at all** on the one report whose purpose is to say who is in which unit. `leases.tenant_name` / `tenant_phone` are the tenant OF RECORD; the view coalesces the joined user **first**, so a portal identity always wins and these are only ever read when there is nothing to win against. Creating account-less `users` rows instead would have put a non-person into the table that role, invitation and RLS all key off.
>     - **`import_tenancies` is SECURITY INVOKER, and that is the whole of its access control.** `leases_write` (`0090`) already says: the caller's org, plus `leases.write`, plus oversight-or-`current_user_property_ids()`. A definer function would have had to restate all three, and a restated policy is a policy with two versions — what `0184` had to go back and fix on `tickets_select`, and what decision 8 forbids in general. Running as the caller means RLS vets every imported row with the identical clause a hand-entered one meets.
>     - **One transaction, all or nothing.** A half-imported rent roll is worse than none: the totals are wrong, the landlord statements are wrong, and nobody can tell which half landed. A failing row names itself and takes the import down with it, which is recoverable. Deliberately unlike the asset importer, which reports per-row outcomes — an asset filed wrong is a wrong label; a tenancy filed wrong bills a real person.
>     - ⚠️ **`leases_no_overlap` cannot make a re-upload idempotent.** It excludes only where the status is `active`/`renewed`, so a draft import sails straight past it and silently doubles the rent roll. The validator carries that check itself, against what is already recorded *and* against what the file has claimed on earlier rows — a file can collide with itself.
>     - **Rows are inserted as `draft` and then put through `activate_lease()`, never written `active` directly.** Writing the status would bypass the one function that owns that transition and leave the unit's occupant unset — decision 22's "occupancy and tenancy disagreeing", produced in bulk.
>     - **What is deliberately NOT importable:** rent billed, rent collected, management fee, landlord net, service charge. Every one is DERIVED from the ledger by `tenancy_schedule`, and a column here would be a second place for a figure the ledger already owns. The workbook carries them because a spreadsheet has no ledger. Said on the screen, not left to be discovered.
>     - **An email links to an account that already exists and never creates one.** Bulk-creating identities from a spreadsheet is not an import, and an account nobody asked for is an account nobody secures.
>     - 📌 **Gated on `leases.write`, not `records.export`.** Export is a DPA control about PII leaving the platform; this is the opposite direction and it is the letting act at scale, so it takes the letting permission.
>     - 📌 **One CSV parser.** `parseCsvLines` moved from `lib/asset-schema.ts` to `lib/csv.ts`, re-exported so every existing caller is untouched. A second parser is how two importers disagree about a quoted comma, and a generic parser exported from a module named for assets is how the next person writes that second one rather than finding it. Proven by both asset-import suites passing unchanged.
>     - 📌 **And two of this suite's own checks passed for the wrong reason.** "Refused by RLS" was actually `0225`'s trigger raising *"That unit does not exist"* — its `SELECT` on `units` runs as the caller and finds nothing on a property they do not hold. A true refusal, one layer earlier than the one being named. It now proves the SCOPE is what decides by *moving* it: stake the manager to that property and the identical row lands; un-stake them and it is refused again. Separately, section F read `tenancy_schedule` through the **service role** — which is `security_invoker` and neither oversight nor a stakeholder, so it returned zero rows and three assertions read `undefined` and failed as though the feature were broken.
>     - 📌 **The template's own guidance row reported an error against itself** — "You do not manage a property with this name", pointed at a row of instructions, on the one file every first-time user starts from. It is recognised and skipped now. "Delete this row first" is not an instruction people follow.
>     - `0265`, verified by `scripts/verify-tenancy-import.mjs` (36 checks), with portfolio-and-controls, asset-import and asset-import-e2e re-run unchanged.
>     - 📌 **Related, same turn: where a reply to a client-facing email goes.** Asked because every one is sent From `no-reply@notify.<brand>` — a dedicated sending subdomain that is deliberately not a mailbox — while the copy says "reply to this email". It works: a `Reply-To` header points at a real monitored inbox chosen by category, and every mainstream client honours it over `From`. What was wrong is that the ADMINISTRATOR could not see it. Leaving "Finance / accounts email" blank silently routes every invoice, statement, receipt and payment-request reply into the general support inbox — measured on staging, OEA has no finance address, so a tenant replying "I have already paid this" landed in `info@`. The rule moved to `lib/mail-routes.ts` (pure, so a client component can read it) and Settings now shows, live as somebody types, exactly where each kind of reply will land and when support is catching something on another desk's behalf. **The value stays the org admin's to set; what was missing was seeing what it does.**

> 38. **The chain being complete is a fact about the payment, not a licence for whoever can reach the row (v3.7, 7 Sept 2026).** A full `npm run verify` came back 21 red of 114. Nineteen of those were the suite estate rather than the product, and separating the two is the whole of this entry — but two were real, and one of them is on the money path.
>     - ⚠️ **An FM/PM could mark a payment `approved` and write themselves into `approved_by` (`0266`).** `payments_update` (`0078a`) admits `fm_roles()` for vendors in their own scope, and it has to — setting `service_verified_at` is the operational half of the B4 gate. The status column travels on the same policy, so the only thing between that desk and `approved` is `enforce_payment_transition`, and every other consequential transition there names its role while `recommended -> approved` named none: it tested `is_cleared_for_disbursement` and nothing about the caller. **The permitted desk is now read from `payment_chain_stages(org)`** — the final stage's own roles — rather than listed, so decision 28's per-org shapes cannot leave a stale literal behind. No money moves on this by itself (`assert_may_disburse` and decision 16 are untouched); what it forged is the record of who authorised, on the one field an auditor reads to answer that question, four commits after decision 34 finished putting every approval decision INTO the audit trail.
>     - 📌 **And proving the fix found a second door into the same field.** The trigger returns early when the status does not move — correctly — and that return handed back a row whose `approved_by` was still writable, so `update payments set approved_by = <me>` on an ALREADY-approved payment succeeded and the new gate never ran. The early return now carries the stamp forward from OLD, `coalesce`d so a null on an older row can still be filled in: what is closed is overwriting an attribution, never recording one. Decision 24's own summary of itself, one more time.
>     - **A removed operator could still change another organisation's money controls (`0267`).** `operator_set_approval_chain` (`0248`) and `operator_set_approval_tiers` (`0261`) gate on `caller_is_operator_admin()`, which asks for a role and an org and never whether the account is still live. `0195`'s subject was self-scoped READS by a removed tenant; this is a write, one level up. 📌 **`my_notifications` was in the same list and had ALREADY been fixed by `0195` — and came back**, because `0221` replaced it to add a column and was written from the pre-`0195` body. A `create or replace` re-states the whole function, and whatever a previous migration put there that the author did not know about is gone. The generator now also re-states each rewritten function's grants, read from the live catalogue, because that is the same wound `0204`/`0209`/`0210`/`0264` have now recorded four times.
>     - ⚠️ **Nineteen suites were asserting rules the board had already replaced, and that is not "them working" past the day of the change.** Twenty-five checks across four suites still encoded the tier ladder `0261` switched off two days earlier; eight more across three suites still held decision 9's "a regional manager touches nothing financial" against decision 26; one held decision 18's "the two manager guides are identical" against decision 29; one held `hierarchy.write` as admin-only against decision 26. **A board decision is not delivered when the migration lands. It is delivered when every suite that asserts the old rule has been re-read** — the same sentence decision 24 wrote about consumers of a schema change, applied to the estate that is supposed to catch exactly this.
>     - **Where a rule became per-organisation, the suites now test BOTH modes rather than whichever the data happens to be in.** `verify-approval-chain` turns the bands ON for the run — it is the ladder's own suite — and a new section 13 asserts the tierless default: tier 1 clears an amount it could not clear a moment earlier, and finance, an FM and an administrator still hold no stage. `verify-finance-journey` and `verify-ops-requisitions` ask the org's flag instead of assuming. 📌 `verify-analytics-drilldown` had drifted in the direction that matters — its mirror of `canActorAction` still mapped `admin` to tier 2, which decision 23 removed on 28 Aug, so **the thing built to catch queue-vs-database drift was itself offering an administrator a payment the database would refuse**. Its expectations are now written out per mode rather than being the mirror's own formula on both sides of the comparison, which would have passed whatever either side did.
>     - 📌 **Four reds were the suites' own fixtures, and each one had been reading as a product fault.** `verify-ticket-reference-search` filed its ticket on any property in the org and the POC manager manages none, so a working scope read as a broken lookup — and the RLS section it exists for passed throughout, which is how a fixture fault gets mistaken for a product one: the alarm sounds on the half that was never the point. `verify-reconciliation`'s teardown deleted an entry's postings and left the entry, which `0256` correctly refuses; the refusal was right and the cleanup was wrong, and a fixture that writes a balanced pair must remove it as a pair. `verify-lettings-grants` could not classify `approval_chain_shape` / `approval_tiers_enabled` — both correctly absent from the `0083c` allowlist, both now named and both ATTEMPTED, because "not granted" and "nobody decided" look identical from that side of the line. And `verify-fm-journey` judged an approval attempt by whether the statement threw, when an update refused by RLS affects zero rows and raises nothing — it now reads the state afterwards.
>     - ⚠️ **Three suites were killed by the runner's 300-second budget and passed standalone.** `verify-conversational-intelligence` calls the router model once per message; `verify-notification-links` and `verify-role-workflows` walk every role in every org, so like `verify-finance-journey` their runtime grows with the client list rather than with the code. All three are now in `SLOW`. **A budget that fails a working suite is worse than no budget** — it produces a red that survives investigation only until somebody re-runs the file, which teaches people to re-run rather than to read. `verify-intake-intelligence` died on an unhandled ECONNREFUSED and reported `Error.captureStackTrace(err)` as its summary; it now prints `Cannot reach <url>` like `verify-checkout-e2e`, so the runner marks it SKIP and a missing precondition stops looking like a broken classifier.
>     - 📌 **And two suites failed on ANOTHER suite's litter.** Staging carries **214 live `probe*@oegroup.test` accounts** left by runs that died before their teardown, holding real roles — `admin`, `executive`, `regional_manager` — in real organisations. An unordered `.limit(1)` on role alone returned one of them and the run ended on "Invalid login credentials". Both pickers now exclude the prefix and order deterministically, which is `scripts/lib/org-lookup.mjs`'s recorded lesson about `delivery_brand` arriving from a third direction. **The accounts themselves are still there and are owed a sweep**; a fixture that holds a role is not litter, it is an access grant nobody decided to make.
>     - Two real defects, two small gaps (a server action throwing its reasons where Next replaces them with a digest; three capabilities and a screen with no training process), and seventeen suites brought back into step with the board. `0266`/`0267`, with approval-chain, finance-journey, oea-payment-chain, invoice-appeal, remittance, payment-gate-authority, payment-approver-reach, deactivation, function-grants and the access suites re-run.

> 39. **A request carries who raised it, in what capacity, and where — reliably (v3.7, 7/8 Sept 2026).** Asked directly: "all requests coming from a tenant/vendor/landlord/fm/pm/rm should be accompanied with the role/properties/ids such as attached property/unit pertaining strictly to the request, user role/id, etc." Measured against every path that actually creates a `tickets` row before writing anything, and found the same shape twice: `property_id` resolved from `units.occupant_user_id` — decision 22/0226's own finding, one screen over, and for a **landlord** it resolved to nothing at all, since an owner never occupies a unit even though decision 19 gives them their own raise path. `unit_id` (0269) was written by nobody. Role was on no row at all — `app/dashboard/tickets/[id]/page.tsx` derived "is this a tenant" from `sender_id === viewer.id`, the exact inference 0218 already found wrong once.
>     - **`tickets.sender_role`** — a snapshot, not a live join, matching this schema's own pattern for a fact that must not silently reinterpret itself later (decision 14's fee %, decision 30's superseded approvals). Historical rows left NULL rather than backfilled from a role that may not be the one held at the time (0221's own precedent).
>     - ⚠️ **Found while writing the portal insert, not assumed: `tickets_insert`'s WITH CHECK constrains only `org_id` and `sender_id`.** Every other column — including the new `sender_role` — was client-settable by any signed-in caller via a direct REST call, bypassing the server action entirely. A snapshot whose whole purpose is trustworthy attribution is not one if a tenant can set it to `'admin'`. Fixed the way this schema already fixes exactly this shape (`enforce_approval_rules`'s own reasoning for `actor_role`/`actor_tier`): a `BEFORE INSERT` trigger overwrites `sender_role` from `current_user_role()` on every authenticated insert, so no write path — present or future — can leave it as whatever the client sent. `property_id`/`unit_id` carry the same structural exposure and are **not** widened by this migration — a validated resolver already picks them in every path that matters, and closing that gap generally would mean routing the portal's insert through a SECURITY DEFINER function the way `raise_work_order` already does, which is a larger change than what was asked and is recorded here as owed, not silently fixed.
>     - **`resolve_chat_sender`** tries the sender's live LEASE first — the same fact `caller_is_tenant_of_place`/`my_tenancies()` already trust, and the one decision 226 found more reliable (16 of 18 real tenancies then had no matching occupant) — and falls back to recorded occupancy only when no lease resolves, kept rather than replaced because a company let can have an occupant with no portal login of their own (decision 22). Now also returns `unit_id` and `role`. Grants re-asserted explicitly (`service_role` only, not even `authenticated` — the one function reached with no session behind it at all) after the return-type change forced a drop-and-recreate, the fourth recurrence of 0204/0209/0210/0264's lesson.
>     - **`raise_work_order`** gains an optional `p_unit_id`, validated against the property already being validated, and stamps `sender_role`. ⚠️ Adding a trailing DEFAULT parameter still changes a function's identity in Postgres — `create or replace` alone left TWO callable overloads (7-arg and 8-arg) rather than replacing one, exactly the "two ways to approve, one of them the behaviour being replaced" shape 0263 already named. Caught by the migration failing on "function name is not unique" rather than by reading; fixed with an explicit `drop function` first.
>     - **The portal** (`raiseRequest`) resolves a tenant's place from their own verified live lease — never from client input, which for a tenant is trusted from nowhere else. A landlord or staff member raising through this same generic form gets an optional property→unit picker sourced from `current_user_property_ids()` (the same resolver `raise_work_order` already trusts), **re-verified server-side under the caller's own RLS session before being trusted** — a client-supplied id that is not genuinely theirs comes back empty and is silently dropped.
>     - **A tenant with more than one live tenancy** gets a required picker naming which of their own homes a request is about, rather than the property/unit landing on nothing. Not a hypothetical: measured live on staging, one seeded commercial lessee carries 18 tenancies, and the free-text hint this form has always had cannot disambiguate that.
>     - **The staff ticket detail page** showed only the AI's free-text guess (`property_or_unit`) in its "Property / Unit" field, never the resolved value even where one existed. Now shows the resolved property/unit first, falling back to the free text only where nothing resolved, and gained a "Raised by" field reading the new snapshot.
>     - Verified end to end through the browser rather than assumed: a multi-tenancy tenant picking one of 18 homes, an FM/PM raising work against a specific unit, and a landlord raising against their own property — each confirmed correct at the database level (`sender_role`, `property_id`, `unit_id`) after submission, plus the tamper attempt (a signed-in tenant claiming `sender_role: 'admin'` via a direct table insert, overwritten to `'tenant'` by the trigger) and the lease-preferred/occupancy-fallback split on `resolve_chat_sender`, both proven with isolated fixtures.
>     - 📌 **Running `verify-fm-journey` — its first run since `0271` landed — caught a self-inflicted wording drift, not a behaviour bug.** `0271`'s own header claimed its rebuild of `submit_vendor_evaluation` was mechanical ("moved and not retyped"); one line was retyped anyway, changing the tenant-guard refusal from its established wording ("the satisfaction rating belongs to the tenant who reported this — rate the quality of the work instead", stable since `0220`) to a different sentence that dropped the "rate the quality instead" guidance. The refusal itself was correct throughout; only the words a person reads had drifted, and `0271`'s own verification never touched this specific line. `0274` restores the exact wording and asserts it in the migration, so the next silent retype fails the build rather than a suite run days later.
>     - `0273`/`0274`, verified against `verify-chat-request-visibility`, `verify-fm-journey`, `verify-deactivation`, `verify-function-grants`.

> 40. **A link that resolves is not thereby a destination (v3.7, 8 Sept 2026).** Two reports, one sentence apart: the tenant's own home indicator on My Requests "should be clickable and point to my rent", and *"the notification on tenancy expiry does not currently point to anywhere, can it be updated to point to an actual destination where the action expected in the destination can be taken?"* Both are the same fault at two different depths.
>     - **The header line named a place and did nothing.** Decision 39's turn folded "where do I live" into the page description and made only the `+N more` overflow a link — so on the 16 accounts with one or two tenancies, the one thing on the page styled as a fact about *them* was inert. The whole indicator is the link now. ⚠️ It stays composed of inline elements only: `PageHeader` renders `description` inside a `<p>`, so anything block-level here is invalid HTML that renders anyway and reflows unpredictably.
>     - ⚠️ **The renewal notice was NOT a dangling link, which is why nothing caught it.** `verify-notification-links` passed on the day it was reported, correctly: the row carries `entity_type = 'lease'`, a live `entity_id`, and a `link` of `/dashboard`, which resolves. The suite watches for links pointing at *nothing*; this one pointed at *something irrelevant*. A tenant was told their tenancy ends in sixty days and was put on a screen that mentions neither the tenancy nor the date nor anything to do about it — while the notice itself had known which lease it was about the whole time.
>     - **The destination is that lease's own page**, which `leases_select` (`0090`) has admitted the tenancy's own tenant to since it was written. **No access was widened** — a link was pointed at the row it was already about. Two dead ends behind it had to be closed with it: the page's "Back" went to `/dashboard/leases`, which is gated to the letting desk and oversight, so a tenant arriving from the notice met a refusal at the end of the fix.
>     - **The ACTION had to exist for the destination to be worth pointing at**, and it is deliberately not a self-service renewal. A renewal is an offer the organisation makes on terms a person states (decision 36); a button that renewed a tenancy would be the rubber stamp decision 10 forbids, from the lettings side. What a tenant can do is say which way they are going, in writing, early enough to matter — so the page carries the term, the end date, the escalation recorded against a renewal, and two controls that raise an ordinary service request **pre-addressed to that tenancy**, arriving with the property and unit attached (decision 39) rather than as an unattached message about "my flat".
>     - **The opener is a closed set, matched exactly, composed server-side from the tenancy's own row** — never free text lifted out of the URL. This form's output is a `tickets` row raised in the caller's own name, so a `?message=` parameter would let one person hand another a link that files a request saying whatever they wrote. The link chooses *which* sentence; the server owns *what it says*; the person edits it before sending. A `lease` they do not hold, or an `about` naming no known opener, produces the plain empty form — proven both ways in the browser.
>     - **The panel appears on the org's own notice thresholds** (`orgs.renewal_notice_days`, decision 15), read rather than hardcoded, so what a tenant is shown and when they are written to cannot drift apart. It does not appear on a terminated or expired tenancy: you do not offer to renew something that has ended, and the page is still a full statement without it.
>     - 📌 **A fix that only applies going forward is not a fix for the person who reported it** (`0221`'s own words). The notice on the screen in the report is a row already written, and the job runs once per (lease, threshold) by design — `lease_notices` has claimed that pair since `0093`, so no re-run would ever repair it. `0275` repoints it. The stored *body* is left alone: rewriting the text of a notice already sent is rewriting a record.
>     - ⚠️ **And writing that migration found the real orphan (`0276`).** Its assertion failed on `dev` with a lease notification whose lease has been hard-deleted. `0138` built `delete_notifications_for_deleted_entity()` as ONE parameterised function precisely so this could not happen — and then wired it to `tickets` and `payments`, the two tables the reported 404s came from; `0242` added `tenant_applications` when a third arrived. **`leases`, `assets` and `properties` were never wired**, though all three are first-class notification subjects: `my_notifications`'s `target_live` case lists six entity types and `verify-notification-links` section B walks the same six. The mechanism was general and the wiring was written once per report. `0276` closes all three and asserts the **enumeration** rather than this batch — a seventh subject with no cascade behind it now fails the migration that adds it.
>     - 📌 **A suite is only evidence about the database it was pointed at.** Section B is correct and would have gone red — on `dev`. The suites read `.env.local`, which points at staging, where no lease had been hard-deleted. Four worlds, one world tested.
>     - 📌 **And re-running `verify-tenancy-statement` caught decision 38's own subject again, four days later.** Its §G asserted *"a vendor still reads no properties"* — written when `0226` was the only branch beside the staff ones, and superseded on 7 Sept by `0269`, which the board asked for so a contractor can see the building they are dispatched to while the job is live. `0269` shipped **without a suite of its own**, so this was the only place in the estate watching that boundary, and it was watching for the absence of the feature. It now tests the rule that exists in both directions — every property the vendor reads is a live job of theirs, and one they hold no live job on comes back as no row. Relaxing it to "any number is fine" would have stopped watching the boundary, which is worse than the red.
>     - `0275`/`0276`, verified with `verify-notification-links`, `verify-lease-notices` and `verify-tenancy-statement`, plus the whole path walked in the browser as the tenant: the reported notification opening its own tenancy, the panel on a live one, both openers composed correctly, the request landing with `sender_role = tenant` and the right property and unit out of eighteen, and a forged `lease`/`about` producing nothing.

> 41. **A rebuild that was not a rebuild, and a sweep that could never succeed (v3.7, 8 Sept 2026).** A full `npm run verify` came back 3 red of 115. One was decision 40's own (a suite asserting a rule `0269` replaced). The other two were both real, and the first is the most serious defect this build has recorded.
>     - ⚠️ **`submit_vendor_evaluation` had been completely non-functional since `0271`.** Not degraded — no evaluation, from any source, in any organisation, could be submitted at all, and B4 states plainly that no vendor payment executes without a performance evaluation. The one control between a contractor and their money had been raising an error for anybody who tried to clear it. Three faults, all the same act: it inserted into `evaluation_responses (… value, points)`, columns that **have never existed** (`0104` created the table with `response_value`/`points_awarded` and it has never been altered); it called **`recompute_evaluation_scores()`, which does not exist and never has** — named in no migration but `0271` and `0274` — in place of `0234`'s inline scoring, both the manual dimensions and the response/completion taper against the SLA target active when the ticket resolved; and it **silently reverted `0234`**, whose entire subject was refusing a payload that answers one criterion twice.
>     - 📌 **This is `0183`'s rule failing in the worst available way, and `0274` walked straight past it.** `0271`'s header claimed its body was catalogue output, *"moved and not retyped"*. It was written from memory — memory that was wrong about a table's columns, invented a helper, and dropped a control added eleven days earlier. `0274` was written *specifically because* `0271` had retyped one line, and it verified that line, asserted that line, and carried the rest of the wreck forward unread. **A fix aimed at the instance rather than the class is how the class survives.** `0277` restores `0234`'s body verbatim with exactly `0271`'s three intended additions (the comment parameter, its length guard, the column on the insert), and asserts the body against the **catalogue** — every column it writes must exist, nothing it calls may be absent, and `0234`'s and `0220`'s controls must both still be there.
>     - 📌 **Three suites passed throughout, and each for a reason worth naming.** `verify-vendor-journey` and `verify-vendor-score-consumers` write through the service role straight into the tables; `verify-fm-journey` exercises only the REFUSAL path, which returns before the insert. That is `0216`'s recorded lesson exactly — **a suite that proves the policy and never sits in the user's seat** — and it is why the one suite that calls the RPC was the only red.
>     - ⚠️ **`verify-vendor-self-service` was not slow; it was failing at something, for ~260 seconds, on every run.** Its start-of-run sweep deleted stale probe accounts one at a time, and its third call is **refused by design**: `audit_log_actor_id_fkey`, because a probe account that ever acted has an audit row and the trail is immutable. So the backlog was not a queue that drains — it was permanent, 137 rows, re-attempted in full every run at ~1.9s each, before the first check printed. The runner killed it at 300s, and the kill meant the suite's own teardown never ran either, so each run added a few more. From outside it read as "the suite hangs".
>     - **The teardown was where the 137 came from.** It ignored the delete's error, so a probe user that acted during the run simply stayed — holding a real role in a real organisation. Decision 38's own sentence: *a fixture that holds a role is not litter, it is an access grant nobody decided to make.* Both ends now do what the platform supports: try the delete once, in bulk, and **deactivate** whatever the audit trail refuses to release (`0194`/`0195`'s remedy, and the one this repo already chose for exactly these accounts). All 143 are now deactivated, none live, and the suite runs in **72 seconds**.
>     - 📌 **It is deliberately NOT in the runner's `SLOW` set.** Naming it would have made the red go away and hidden the fault — a budget that fails a working suite is worse than no budget, and a budget raised to accommodate a suite that is failing at something is worse still.
>     - `0277`/`0278`, with vendor-evaluation, fm-journey, vendor-journey, vendor-score-consumers, function-grants, money-function-grants and tenancy-statement re-run.
>     - **And the notice's stored body now matches where its link goes.** `0275` repointed it and left the text reading *"Speak to the letting team if you would like to renew."* I declined to change it as rewriting a record; asked for it directly, and the objection does not hold for this row: `user_notifications` is the in-app bell, not the record of what was sent. What was sent is retained untouched — `lease_notices` holds the claim, recipient, channel and delivery outcome per (lease, threshold), `audit_log` is immutable by trigger, and the email itself is in the tenant's inbox beyond this schema's reach. **A bell item whose words contradict its own destination is the defect, not the remedy.** `0278` replaces only that clause, and asserts that the unit and property it names survived the edit.

> 42. **Who may invite whom is stated, not derived — and the form must offer what the rule allows (board, 8 Sept 2026).** Asked as: *"only admin should have the privilege to invite everyone and none other, not the regional manager, not any other role. The regional manager may be able to add fm/pm/vendor/tenant/property-owner only, but not executive, admin or any other role."*
>     - **The escalation half was already held, and the form was the fault.** `0078c`'s rank rule already made `executive`, `admin` and all three payment desks unreachable to a regional manager. What the screenshot showed was `InviteDialog` offering **every role except `admin`** to an FM, a PM and a regional manager alike — the Managing Partner, the payment officer, the auditor and the approver among them. The database refused all of them, so nothing could be minted; what happened instead is that a manager picked "Managing Partner", filled the form and was told no at the end. `invitableBy()` had been written, exported and **never called here**. Decision 26's fault exactly — *the nav and the page disagreed, and the page was the wrong one* — with the roles swapped.
>     - **`invitable_roles(inviter)` is the one place the question is answered** (`0279`). The administrator's answer is read from `enum_range`, so a role added tomorrow is an administrator's to issue and nobody else's by default — decision 7's "most restrictive workable state" applied to this question. The regional manager's is a **list**, not a rank: a rank is one number consulted by three files, and the roles it must never reach are exactly the ones where being one number wrong is an escalation.
>     - 📌 **`fm_ops_staff` is out, and that is a real narrowing.** Decision 9 says the regional manager exists partly to invite "operational staff", which is what that role is; the instruction enumerated five roles and said "only", so the narrower, reversible reading was taken. Restoring it is one array element in `invitable_roles()` and one in `lib/roles.ts`, and `verify-role-hierarchy`'s check moves from section C back to section B.
>     - 📌 **A role that may not invite now answers with nothing.** The first draft fell through to the rank for every remaining role, so `invitable_roles('finance_approver')` claimed they could issue a `payment_approver` and a `regional_manager`. Harmless — `invitations_insert` has admitted only `admin` and `fm_roles()` since `0078c` — and wrong as an *answer*, which is how a rank stops summarising the rule and becomes a second, disagreeing statement of it. Caught by the migration's own assertion.
>     - ⚠️ **And then I rebuilt the policy from a copy three migrations stale (`0280`).** `0279` replaced `invitations_insert` to change one clause and took the rest from **`0078d`**, dropping every clause `0081` had added since: that each property in `property_ids` is one the inviter may attach to, that a tenant's `unit_id` sits on such a property, and that a vendor's `vendor_id` is within their own scoping. For the minutes between the two migrations a manager could have planted someone on a property outside their region. **This is `0183`'s rule broken in the file written immediately after `0277`, whose own header is four paragraphs about exactly this.** Knowing the rule is not the control; reading the live object is, and there is no version of "I only changed one line" that makes the other lines somebody else's problem. Caught by `verify-role-hierarchy` §H within a minute — the migration's own assertions all passed, because they tested the clause I was thinking about. `0081`'s header says it in advance: *a test that exercises the field you were thinking about confirms the thought, not the boundary.* `0280` restores the policy in full and asserts every scope-bearing column is still named in the live expression.
>     - `0279`/`0280`, verified by `verify-role-hierarchy` (rewritten for the stated set, both directions, plus the whole-enum matrix), with invitations, invite-acceptance, permissions, access-matrix and role-surface re-run, and the regional manager's own dropdown read in the browser: five options where the report showed twelve.
>     - 📌 **Related, same turn: the runner's summary column.** Fourteen green suites reported *"(no summary line — the suite printed nothing recognisable)"*, which reads as a suite that passed and forgot to say so, and teaches you to stop reading the one column that says whether a suite asserted anything. Two causes. **The regex was matching text that still carried its formatting** — every sign-off is printed as `\n\x1b[32mAll … passed.`, so the `^` in the pattern written to catch these anchored to the ESCAPE SEQUENCE and never to `All`. The colour is stripped before matching now. And the suites themselves now sign off in one shape, `ALL CHECKS PASSED — <what a green run proved>`, so the column reads as a column.
>     - ⚠️ **Two of the fourteen could not fail at all.** `verify-account-recovery` and `verify-completion-evidence` state each result as printed text — `console.log(label, err ? "FAIL …" : "PASS …")` — and counted none of them, so they exited 0 whatever they found and the runner, which reads the exit code, reported a red run as green. On the one self-service path that changes an authentication credential. **The missing summary was the symptom; a suite that cannot fail was the fault.** Both now count from their own OUTPUT rather than by restating a dozen assertions through a helper — rewriting call sites to change how a result is reported is a dozen chances to change what is asserted, and this way the exit code and the screen cannot disagree.
>     - 📌 And `verify-bi-scoping` and `verify-schema` assert nothing whatever — they print a table for a person to read. They now say **DEMONSTRATION ONLY**, the marker `verify-cascade` already carries, rather than accepting a green PASS for coverage they do not provide. What they display is enforced by `verify-access-matrix`/`verify-role-surface` and `verify-security-posture`/`verify-rls-rest` respectively.

> 43. **A regional manager's region is part of who they are on the roster, not three clicks away on the hierarchy screen (8 Sept 2026).** Asked directly: *"the regional managers when assigned should have their regions as part of their portfolio in bracket eg REGIONAL MANAGER (NORTH)."* Nothing new to grant or resolve — `stakeholder_assignments` (`0067`) already turns a node into a readable label, definer-free, through the caller's own policies. This is the one place it had never been read from: the roster that names the person.
>     - **Members and pending invitations both carry it.** `/dashboard/people` groups `stakeholder_assignments` by `user_id` for every `regional_manager` row and appends the joined labels in brackets — `portfolioLabel()`, new in `lib/roles.ts`, silent for every other role and for a regional manager who holds no assignment at all ("when assigned" is the request's own qualifier; a placeholder like "Unassigned" would be inventing a fact the hierarchy screen's own empty-state line already states better). The pending-invitations list carries the same bracket one step earlier, from `invitations.node_id` — walked against the tree already fetched for the picker, so it costs no extra query.
>     - **A regional manager can hold more than one node** (`0067`'s uniqueness is per node, not per person — confirmed live: one OEA account holds both a region and a location under a *different* region), so the bracket is a joined list, never assumed singular. And a node can be at any level decision 9 allows (region, project, location, site), so the bracket reads `node_full_name()`'s full ancestor path rather than the node's own name alone — "Abuja / Maitama / TEST PROJECT / OEA New Test Site" for a site-level assignment, plain "South" for a region-level one, so the badge always states the actual portfolio and not only its top.
>     - ⚠️ **Found by looking at the rendered roster, not by reasoning about the query: a regional manager can also hold an ordinary property-id attaché row**, `node_id` null, alongside their node assignment — one live account carries one dated a week before this turn, origin unclear. `stakeholder_assignments.scope_label` resolves that row to the PROPERTY's own name, and the first draft, filtering only on role, rendered it as though it were the region: *"Regional Properties Manager (Lake River)"* — the wrong fact in the exact shape the bracket exists to state correctly, which is worse than an omission because a reader has no reason to doubt it. `.not("node_id", "is", null)` closes it; caught by reading the actual page before calling the feature done, the same discipline `0216`/`0225` were written to record.
>     - No migration: the view, the resolver and every grant already existed. Verified end to end in the browser rather than assumed — a real multi-region account, a real narrower-than-region account, the property-attaché false positive reproduced and then fixed, and a live invitation issued, read on the pending list, and revoked.

> 44. **The fund-override control had no screen on one of its two payable types, and the account it needed did not survive its own refusal (9 Sept 2026).** Reported live, on the exact refusal `0272` was written to answer: *"Olayinka's Estate's service-charge fund cannot cover this… Nothing has been sent."* — after the override had already shipped.
>     - ⚠️ **The control was reachable from exactly one of the two payable types it was built for.** `authoriseShortFund` (`0272`) has taken `payableType: "vendor_payment" | "ops_requisition"` since it was written, and the database side has resolved a vendor payment's account correctly the whole time — but the function was only ever CALLED from the requisition's own send flow (`SendLineGroup.tsx`). A vendor payment hitting the identical refusal on `/dashboard/payments/<id>` had no control to reach it at all, on any run of the app, ever. `PaymentActions.tsx` now carries the identical panel (same wording, same 20-character floor, same single-use consequence), wired to the same action.
>     - ⚠️ **And once reachable, authorising it still refused — for a second, different reason.** With the panel in place, authorising against the reported payment answered *"This payment has no property fund to authorise against"* — worded for an unattached payment, on one that plainly names a building. Traced to `ensure_property_ledger_account` (`0247`): it opens a property's fund sub-account **lazily, inside the very transaction that needed it** — and the transaction that needs it is the one `assert_funds_available` is about to refuse. The refusal rolls the whole transaction back, sub-account creation included, so the account is created and destroyed in the same breath on every short-fund refusal. `payable_fund_override_state`'s own account lookup was a passive read for a row that transaction never leaves behind. Measured live: **2 of the platform's properties** had a persisted sub-account (Osborne Tower, PROBEFUND-Tower-QO92O) — the only two that had ever had a SUCCESSFUL posting create one. Every other property's override was unreachable by construction, which is the majority case, not an edge one.
>     - 📌 **The same shape decision 24 keeps finding.** A helper written for the case in front of the author (an account that already exists) and silently wrong for the one that wasn't (a property whose fund has never yet run short before — which is exactly when the override is needed). `payable_fund_override_state` (`0283`) now asks for the account the way a real posting would — through `ensure_property_ledger_account` itself, which opens it if absent — rather than a read that can only ever succeed after something has already survived to prove the account exists.
>     - 📌 **Verified against the exact reported payment, not a fixture standing in for it.** Signed in as OEA's real payment officer, against the real `27d8201a…` payment (₦350,000, Olayinka's Estate): the panel now renders, the reason is accepted, the override authorises, and the ledger posting the segregation gate exists to police goes through — consuming the authorisation, exactly once, exactly as designed. The one thing that then failed was the actual bank transfer, on `Invalid key` from Paystack — a local-dev-environment credential, unrelated to this fix and not reproducible on the real deployment, where Vercel holds the real key. The payment was left `approved`, unremitted, safe to retry.
>     - ⚠️ **And fixing it broke a suite for a reason worth recording, because it was not this migration's fault.** `verify-fund-override` §E drains ₦99,000,000 out of the REAL `oea` org's client-funds account to prove an overdraft refusal holds even with a live override standing elsewhere — and that org's real balance has grown past ₦121,000,000 through ordinary use since the figure was chosen, so the drain no longer overdraws anything and the absent refusal was correct, not a defect. This is the identical fault the suite's own comment already recorded once, for the OTHER leg of this exact check ("reused the fund THIS SUITE had already spent down") — fixed there by moving to a dedicated fixture account, and left unfixed here because client-funds is genuinely one shared account per org by design, so a dedicated duplicate is not the answer. The fix instead reads the account's live balance and drains past it by a safe margin, so the check means what it says regardless of how much real activity the org has accumulated by the time it runs.
>     - `0283`, verified by `verify-fund-override` (now green start to finish), with `verify-ledger`, `verify-reconciliation`, `verify-oea-payment-chain`, `verify-function-grants` and `verify-money-function-grants` re-run unchanged.
>     - 📌 **This entry said `0281` in both places above until 9 Sept 2026, and the file it names is `0283`.** The migration was written as `0281`, renumbered on finding a concurrent session had claimed that number for something unrelated (its own header records this), and the prose here was not moved with it. Harmless while nothing else was called `0281`; actively misleading the moment something was — which is now. **A migration number in the board record is a pointer, and a pointer nobody re-read after a rename is how the next person traces a control to the wrong file.**

> 45. **A payment made off-platform is recorded with its proof, and three desks turn it into money (board, 9 Sept 2026).** Asked for directly: an optional rent / lease / service-charge payment route by **direct bank transfer or physical in-bank deposit**, with attaching clear proof and stating the amount both **compulsory**, the amount captured and **computed correctly against the thing it paid for**, a confirmation path **shaped like the approval chain** with every payment detail — proof included — visible to the confirmers (auditor, executive, Payment Officer), the **Payment Officer performing the final confirmation that writes the ledger**, a free-text note/description field for the payer, and the transaction details **printable by the client on submission and by every confirmer up to the ledger update**.
>     - **The gap was real and it was the ordinary case in this market.** Every route money could take into this platform ended at a gateway: `payment_intents` (`0032`) → Paystack/Flutterwave → the HMAC webhook → `record_collection`. A tenant who transferred straight into OE Group's designated client-funds account — the account decision 2 requires and which is printed on every demand — produced no intent, no webhook and no posting. Their statement went on showing arrears they had already cleared, the rent roll understated collections, and the landlord's remittance was short. The money was in the bank and the product did not know.
>     - ⚠️ **A recorded claim is not a payment, and that separation is the whole design.** Nothing reaches the ledger, reduces arrears or becomes remittable until the chain completes. Three reasons, all pointing one way: an unverified assertion must never silently stop a tenant being chased for money nobody received; rent collected is remittable to a landlord net of fees (B4), so a claim that posted on submission would let a forged teller slip pull real money OUT to a landlord; and decision 36 already records that advance-fee fraud against tenants is ordinary here, where a convincing bank-app screenshot is cheap and the defence is a person checking the designated account — which is what decision 2's daily reconciliation exists for.
>     - **The chain is `payment_audit_approver` → `executive` → `finance_approver`, fixed for every organisation, and only the last one posts.** ⚠️ Deliberately a PARALLEL chain rather than the existing one. `payment_approvals` is already polymorphic, so teaching it a fourth payable would have been the smaller diff and the wrong one: that table's trigger reaches `resolve_required_tier` and `effective_approval_tier`, and its outcomes reach `apply_chain_outcome_to_payment`, `assert_may_disburse` and remittance — every one of them governing money **leaving** the client-funds account. This governs money arriving in it. Decision 16 separated those two acts; one control surface would re-join them, with the tier ladder decision 23 removed from inbound work silently applying to a tenant's rent. **The shapes match because the reasoning matches; the machinery is separate because the money goes the other way.** The stage list takes **no org argument** — that is the statement that it does not vary, against decision 28's per-org outbound ladder.
>     - **`finance_approver`, and NOT `payment_approver`.** Decision 23 renamed that role's *label* to "Payment Officer" and left the enum identifier alone because it is named in 123 files including every money path. The board confirmed `payment_approver` — stage 3 of the *outbound* ladder — is not involved here. Asserted in the migration, because `0183`'s own example of a clause lost to a typo is a money bug.
>     - **Both compulsory halves are table constraints, not form validation.** `claimed_amount not null check (> 0)` and `proof_path not null`, plus a **deferred constraint trigger** requiring the breakdown to name at least one thing and to sum to the amount — the invariant the board's third requirement rests on, since if the lines do not sum to the payment then "computed correctly for the thing paid for" is false of at least one of them. And the proof object must **actually exist in storage**: without that, "proof is compulsory" is a check on a *string*, and a caller reaching the RPC directly could record a payment the audit desk would open and find nothing at.
>     - 📌 **The posting goes through `record_collection` UNCHANGED** — one synthetic `payment_intents` row per allocation line, carrying `gateway = 'manual'`, a value `0032` put in the enum on day one and which nothing had ever written. Not a shortcut: `record_collection` is where the management-fee snapshot is applied (decision 14), where a service charge finds its own property's fund (decision 27), where a part payment leaves the charge `part_paid`, and where `amount_paid` moves. Re-implementing any of that for a second payment method is how the two disagree — the failure this repo has recorded in decisions 23, 24 and `0247` itself. Proven on the real arithmetic: ₦2,400,000 in, ₦240,000 to fee income at the snapshotted 10%, ₦2,160,000 held for the landlord, client funds up by exactly the receipt.
>     - **The maker-checker is per person, per claim — not per role.** Whoever recorded it is barred from every stage. It can legitimately refuse a Payment Officer who took the walk-in themselves; that is the rule working, and the answer is a second pair of hands, which `0142` already said and the suite proves both ways. Consequently the three confirmation desks are **deliberately not granted** `payments.record_offline`: handing a chain role the ability to record would hand them a way to take themselves out of the chain. `finance_approver` is the one deliberate exception, because taking a walk-in at the finance desk is the commonest way one of these arrives.
>     - **A refusal has a way back.** `returned` at stage 1 goes to the recorder as `returned_for_correction`; above stage 1 it retires the desk below's signature and re-climbs. `rejected` is terminal. Correcting a returned claim retires **every** live signature — a signature given on the figures as they were is not a signature on the figures as corrected (decision 30's rule, and `0270`'s). ⚠️ Without this the chain is a trap, and `0170`'s own header already admits what that looks like.
>     - 📌 **The refusal ORDER was wrong, and the suite caught it by asserting the words.** A claim refused at stage 1 answered a stage-2 attempt with *"this payment has 1 earlier stage(s) still to confirm"* — true, and actively misleading, because it invites somebody to go and get stage 1 signed for a payment that is dead. Terminal state is now tested first. Both branches refuse; only one of them explains.
>     - **Printing is the page, not a second document.** `PrintButton`'s own note states the house rule — what a reader needs from a screen like this is the figures they are already looking at, and a separate PDF is a second copy that can disagree with the first. The claim detail page carries a `PrintMasthead` and is the same page for the payer, the auditor, the executive and the Payment Officer, so **nobody prints a different account of the same payment**. ⚠️ Until it posts, both the masthead and the page state **"This is not a receipt"** — the receipt is issued from the ledger (`0253`) at the moment of posting, and a person holding a printout of an *unconfirmed* payment must not be able to mistake it for one.
>     - **Every confirmer sees the whole record at their desk** — reference, payer, breakdown, the payer's own note, the destination account, and the proof itself, on the queue card as well as the detail page. Decision 24's finding verbatim: the Approvals list said "invoice attached" and offered no way to open it. Proven for all three desks, including opening the stored object.
>     - ⚠️ **One predicate answers "who may read this", and it had to be `SECURITY DEFINER`.** The first draft put the rule in the claims policy and had the allocations policy call back into it — but the claims policy must look at allocations to answer the FM/PM place branch, so the two called each other and Postgres refused with *"infinite recursion detected in policy"*. Worse, the fix that suggests itself is wrong: a `SECURITY INVOKER` helper called from the definer readers (a tenant holds no read on `properties`, `units` or `users`) evaluates RLS **as the owner**, bypasses it, and returns true for everybody — a gate that reads correctly in the source and admits the whole table. `may_read_offline_claim` is definer, and the policies and every reader call it.
>     - 📌 **`revoke … from public, anon, authenticated` is still not the list — `service_role` is the fifth instance.** `0264`'s header says "the role that gets left standing is authenticated"; it was one role short. Supabase's default privileges grant EXECUTE to `service_role` too, and this migration's own assertion caught two functions that must never be callable from outside their one caller — the ledger poster, and the internal breakdown writer — still reachable by every server-side path in the app that uses `supabaseAdmin`. **The list is public, anon, authenticated, service_role.** The assertion that found it had itself been written excluding `service_role`, which is why it is now named there too.
>     - 📌 **A seventh notification subject ships with its own orphan cascade.** `0276` wired six entity types and closed with an assertion designed to fail the migration that adds a seventh without one. It matters more than the type list suggests: `my_notifications` decides `target_live` from a CASE whose fall-through is **`else true`**, so an unknown type is reported live forever — the bell keeps offering a link to a claim that is gone, and the suite watching for dangling links sees nothing wrong. **An unlisted type does not fail loudly; it fails by being believed.**
>     - **A payer can see which account to name, and is told the account's name while doing it.** `bank_accounts_select` admits `oversight_roles()` only, so without a definer reader the compulsory destination field was unfillable by the person the feature exists for. Showing the designated account NAME on that form is decision 36's anti-fraud control at the moment it is most useful — somebody who paid an account in another name finds out there, rather than after three desks fail to find the money. Last four digits only (decision 17).
>     - `0281`/`0282`, verified by `scripts/verify-offline-payments.mjs` (62 checks, every act in a real session across five roles), with collections, ledger, notifications, notification-links, permissions, function-grants and money-function-grants re-run unchanged, and the queue, the record form and the detail page rendered at each of the four desks on staging data.
>     - 📌 **Two of this suite's own reds were its fixtures, and each read as a product fault first** — decision 38's recorded pattern, arriving twice in one build. A `/fee/i` regex matched the landlord posting's *"net of fees"* and reported the landlord's figure as a broken fee split. And the tamper check PATCHed a claim that section F had **already confirmed**, so "the status is `confirmed` afterwards" could not tell a refusal from a success — decision 38's own words about an update refused by RLS affecting zero rows and raising nothing, walked into from the one direction it did not cover: reading the state afterwards is necessary and not sufficient if the state was already the one you are testing for.
>     - 📌 **And the fixture periods collided across runs.** `rent_charges_one_per_period` is `unique (lease_id, period_start)`, and a year offset drawn from ninety buckets collides roughly one run in ninety per fixture — which happened, three sections into an otherwise green run, presenting as "the feature is broken". Spread by **days over two centuries** instead. A suite that fails for a reason unrelated to the rule it names is worse than a suite that does not exist, because somebody spends an afternoon on it.

> 46. **Every person the organisation deals with has a page, and every list of them can be searched (11 Sept 2026).** Asked for as *"a navigable staff / landlord / tenant / vendor list … clicked on to reveal all information about the role, not just downloadable"*, then, mid-build, that "Who is attached to this property" and Leases & Rent be searchable.
>     - **People → Directory** (Staff · Tenants · Landlords / owners · Vendors) and **`/dashboard/people/<id>`**, a printable profile: contact and notification channels, the role's own description, the places a staff member holds, a tenant's tenancies (and, for a money-reading desk, what they owe and paid), a landlord's buildings with their statements, a vendor login's company and capabilities, and the requests they raised or were assigned. The four groups are the four CSVs of `/api/records/export` — deliberately, so screen and file cannot disagree about who is a tenant — and the CSV button appears beside the list only where the route would serve it.
>     - ⚠️ **It widens nothing, and that is the design, not a caveat.** Every section is an ordinary query in the viewer's session: `users_select`, `tenancy_schedule`'s audience predicate, `property_stakeholders_select`, `vendors_select`, `tickets_select`, `payment_intents_select`. No migration. The page decides what to show; the database decides what exists to be shown. People's own gate (admin, FM, PM, RM) is untouched, and profile links are offered elsewhere (vendor page, property panel, lease page) only to readers that gate admits — anyone else would follow them into "Not available for your role", decision 26's nav-and-page split.
>     - ⚠️ **A section RLS might empty for the wrong reason is not rendered, rather than rendered empty** — decision 25 on a new screen. A facilities manager holds no money read (decision 29), so a tenant's payments section is absent for them, not "none"; a PM/RM, whose money read is place-scoped, sees it only on a tenant with a tenancy they hold, and its empty state says "on the properties you hold". Request counts say "you can see", never "in total".
>     - **The tenant of record was missing from two more screens.** Decision 37 gave a lease a `tenant_name` for the tenant with no portal account and made `tenancy_schedule` coalesce it — but `rent_roll` (Leases & Rent) and the lease page still read only the joined account, so a company let rendered "Not assigned" there while the schedule named it, and a search by tenant could never find it. Both now read the account first, then the tenant of record. And there was **no way to record one on an existing tenancy** — only the importer wrote the column — so `recordTenantOfRecord` (caller's session: `leases_write` decides, `audit_leases` records; checks the rows it got back, decision 38) closes it. 📌 The durable fix is `rent_roll` coalescing `l.tenant_name` itself; owed.
>     - 📌 **The first draft of the vendor page's people list said "Nobody logs in for this company yet" over a company whose owner was signed in.** `vendor_users` has two foreign keys to `users` (the person and `invited_by`), so the bare `users(...)` embed is ambiguous and PostgREST refused the whole query — which the page rendered as an empty list. `users:user_id(...)`, and an error is now said as one. Decision 25's zero, arrived at through a query error rather than a policy.
>     - ⚠️ **Found, not fixed, and owed:** four live OEA tenancies sit on units soft-deleted on 11 Sept, so they have vanished from Leases & Rent, the schedule and the directory while still live — nothing refuses soft-deleting a unit that holds a tenancy, and the soft-delete is not audited. Recorded for its own change; the four are real client data and their repair is the organisation's call.
>     - Also corrected in passing because a profile now prints them as facts about a person: the regional manager's role description (overtaken by decisions 26 and 42) and the administrator's guide, which still said they may approve within the threshold (removed by decision 23).
>     - Verified by `scripts/verify-people-directory.mjs` (26 checks: DB writes as real users inside a rolled-back transaction, pages read as five real demo accounts across two organisations; needs `npm run dev`), with training-guide, role-guides, role-hierarchy and vendor-roles re-run.

> 47. **An organisation's money, messages and links carry its own identity — or are refused (11 Sept 2026).** Reported as *"this email is supposed to be sent from OEA … but it carries everything TFML"* — a Paystack receipt to an OEA tenant reading **"Total Facilities Management Limited received your payment"**, with TFML's support address — plus a "Continue payment" 404 and a payment still "pending" after the tenant paid.
>     - ⚠️ **The receipt was accurate, which is the serious part.** Paystack writes it from the MERCHANT ACCOUNT the charge ran on. Measured: `org_gateway_credentials` held **0 rows**; `getGatewayForOrg` (0156) fell back to the platform key for any org without one; `payMyRent` and the service-charge checkout did not even call it — they called `getGateway()`, the platform key unconditionally; and the platform key is TFML's merchant account. So every organisation's collections, and every payout through `getGatewayForOrg`, ran through TFML's Paystack balance. In test mode a branding leak; with a live key, decision 2 broken — an OEA tenant's rent settling into TFML's bank account and an OEA landlord paid out of TFML's balance.
>     - **The rule (`0288`): an org's money moves through ITS OWN merchant account.** `orgs.uses_platform_gateway` names the one org that owns the platform key (TFML, on the evidence of that receipt; unique partial index; not in the 0083c UPDATE allowlist). `resolveOrgGateway` returns the org's own credential, else the platform key only for its owner, else the simulated gateway outside production with no real key, else **refuses** with `GatewayNotConnectedError` — pointing the payer at "Bank transfer / pay another way" (0281), never routing them through another organisation. Payouts go through the same resolver. ⚠️ **Consequence, stated plainly: OEA and the client orgs take no online payments and make no gateway payouts until an administrator connects their own Paystack account under Settings → Banking.** That is the correct state; the previous one was paying them through TFML.
>     - **`payment_intents.merchant_account` records which account took each payment**, because verification must use the same one. NULL (every intent before 0288, all platform) is verified on the platform key — a tenant who already paid is recognised, not chased.
>     - **The 404.** The checkout address was written through the tenant's session; `payment_intents_update` admits only admin/finance, so it matched nothing and raised nothing (decision 38's silent write) — `checkout_url` NULL on the reported intent. "Continue payment" then fell back to `/pay/<ref>`, the SIMULATED checkout, which correctly refuses to exist beside a real key. `set_payment_intent_checkout()` is now the one write path (payer or raiser only; gateway hosts only — a column any caller could point anywhere would be a phishing link with our name on it; merchant written once). The client falls back to `/pay/` only for the simulated gateway. `app/not-found.tsx` replaces Next's bare black 404, host-branded like the sign-in doors.
>     - **"Still pending": no real Paystack webhook had ever reached staging** — `gateway_events` held only the suites' simulated events. Paystack's own guidance is to verify on the return from checkout as well; there was no second door. Now `lib/gateway/settle.ts` is ONE settlement path for the webhook, the payer's return (`PaymentReturn` on My Rent and Statements) and finance's manual refresh: resolve by our reference, verify server-to-server on the account that took it, `record_collection` (idempotent, locks the intent), receipt claimed once via `receipt_sent_at`. **Nobody approves an online payment** — the gateway holding the money, checked by us server-to-server, is the evidence; human confirmation is for the off-platform route (decision 45). ⚠️ "We could not ASK the gateway" is reported as `unknown` and acted on by nothing: the first draft folded it into "pending", and "Continue payment" would then have retired a payment the tenant had actually made whenever the gateway was briefly unreachable. 📌 **The webhook URL in each Paystack dashboard must point at `/api/webhooks/payments/paystack` on the deployment** — that is configuration outside this repo and the likely reason none arrived.
>     - **Paystack's `amount` includes the fee when it is passed to the payer** (the receipt: ₦100,002,000 on a ₦100,000,000 demand). The demand is credited `requested_amount`; the ₦2,000 is Paystack's and never reaches client funds.
>     - **The same shape in every other outgoing channel.** The B8 cascade's email step posted to Resend itself From one global `EMAIL_FROM` ("OE Group <noreply@oegroup.example>") — bypassing the per-org sender `lib/email.ts` had long since been fixed to require; it now goes through `sendEmail`. WhatsApp fell back to the shared `WHATSAPP_ACCESS_TOKEN` for a route with no key — on 360dialog the KEY decides which business a message leaves as — and Telegram's fallback applied even to orgs with **no route at all**; both now send only with the route's own credential. And **fourteen link builders** used `NEXT_PUBLIC_SITE_URL ?? host`, which on staging is a generic `*.vercel.app` address: every invitation, renewal notice, application link and Paystack return URL pointed at a host that was nobody's portal, one scheduled job writing one host into every org's letters. `lib/portal-origin.ts` is the one resolver — the request host if it already answers for this org, the org's own bound domain, and never a host bound to another org. Receipt PDF, analytics report and renewal letters now name the organisation (`publicOrgName`), not its portal label (decision 36's "PM PORTAL").
>     - ⚠️ **And the blue front door was a suite.** `verify-custom-domains` rebound the REAL TFML and OEA orgs to `probe-portal.*.test` and restored them in teardown — on every run since 28 Aug, per the audit trail. A full run killed it at 300s on 11 Sept 00:53 before the restore; `oeaportal.com` and `tfmlportal.com` have answered for nobody since, so both brands served the generic OE Group door. Its section E also **retired the real OEA org** and un-retired it — a run dying between the two would have taken the whole OEA portal offline. It now provisions and retires its own `PROBEDOMAIN-*` orgs and touches nothing it did not create. 📌 **The runner is the one process that survives a killed suite**, so `verify-all` now snapshots every real org before the run and diffs after each suite: drift after a finished suite is that suite's FAIL (not restored — a person fixes its teardown); drift after a killed suite is also restored, column by column. Twenty suites write to `orgs`. Network-class failures (`EAI_AGAIN` — three reds in that run, all passing on re-run) are retried once and then listed as NET, never as a failure of the code.
>     - 📌 **Not fixed, because it was refused:** restoring the two domains directly on the live database was blocked by the session's permission guard. `verify-gateway-isolation` §F stays red until `oea.custom_domain = 'oeaportal.com'` and `tfml.custom_domain = 'tfmlportal.com'` are restored (values from the audit trail).
>     - Also, found on the tenant's own screen while testing: My Rent added Naira and dollar demands into one total and printed it in the first demand's currency ("$158,400,000.00"). `totalsByCurrency` states each currency; amounts in different currencies are never added (0103, decision 25).
>     - `0288`, verified by `scripts/verify-gateway-isolation.mjs` (DB checks as real users in a rolled-back transaction; the real resolvers imported and called; a stubbed Paystack response for the fee), with per-org-gateways, channel-routing (its keyless-route expectation corrected), collections, function-grants, money-function-grants, tenant-rent-payment, notifications, security-posture, custom-domains, training-guide and role-guides re-run.


> 11. **Tenancy intake is per-property (v3.3, 29 Jul 2026 board).** The application window becomes `auto` (open iff a vacant unit exists) / `open` (waiting list) / `closed` (refurbishment, dispute), per property, with the org flag as a master switch. Overrides are administrator-only and audited — pure derivation would remove a judgement that belongs to a person. An applicant arriving through a property's own link carries `property_id`, which is what makes property-scoped PM review possible.

> 7. **Permissions are operator-governed, not org-governed (v3.2, 27 Jul 2026).** Role privileges become an admin-toggled **permission matrix** (Day 6.5) rather than role names hardcoded into policy — but the editor lives **only on the OE Group operator portal**. TFML and OEA administrators see the matrix **read-only**; they cannot change what their own staff may reach. This introduces a **platform operator org** (`orgs.is_platform_operator`), distinct from a brand org, and is the single deliberate crossing of the org-isolation boundary — routed through one audited `SECURITY DEFINER` function, never a cross-org policy.
>     - **Non-delegable controls stay hardwired** and never appear as toggles: payment approval (incl. the threshold escalation to admin), remittance execution, ledger read/write, bank configuration, audit visibility, admin invitation, permission editing itself, and channel-route credentials. These are what an auditor checks; they are not preferences.
>     - **Defaults are the most restrictive workable state.** A capability is granted only where **B7** explicitly names the role; where B7 is silent the default is OFF. A new org starts locked down and is opened deliberately.
>     - **B7 remains the approved baseline.** Any deviation is badged with a per-capability diff and a one-click reset, so drift from the board-approved matrix is visible and intentional.

*Supersedes: Master Build Prompt v2.0. Integrates the AURA Upgrade functional specification (6 modules) and the end-to-end IWMS/IFMS brief (live client-facing service-charge administration + third-party vendor payment coordination & remittance).*

---

## PART A — STREAMLINED AI WORKING INSTRUCTIONS (v3)

### A1. Objective
Design, refine, and progressively deliver an AI-powered **Integrated Workplace/Facilities Management System (IWMS)** for OE Group that unifies facilities management (TFML) and property management (OEA) on shared, secure, cloud-native infrastructure — with **service-charge administration** and **third-party vendor payment remittance** brought live for a new client, and an **auditable real-time dashboard accessible to all stakeholders**.

### A2. Operating Principles
1. **Cloud-native, zero self-hosting.** Managed services only. No Docker/VM ops in Phase 1.
2. **WhatsApp-first intake, web portal of record.** Nigerian users live on WhatsApp; the portal is the system of record.
3. **Two brands, one backend.** TFML and OEA share infrastructure but are fully isolated (routing, JWT claims, database row-level security, API middleware).
4. **AI where it removes toil, humans where judgement matters.** AI triages, classifies, drafts, and reconciles; people approve payments, sign off works, and own exceptions.
5. **Nigerian context first.** Power instability, connectivity, Naira-first payments, NDPA compliance, local support.
6. **Progressive delivery.** Ship a working slice, prove value, then expand. Predictive maintenance is the AI showcase, not the starting dependency.
7. **Evidence over assertion.** Every claim about cost, tool, or timeline is grounded in a source or flagged as an assumption to confirm.

### A3. Guardrails (non-negotiable)
- **Data privacy & security:** encryption in transit and at rest, secure APIs, role-based access, secrets in a managed vault, immutable audit logs (soft-delete only).
- **Compliance:** Nigeria Data Protection Act (NDPA) 2023 + GDPR alignment for international clients; designate a DPO; maintain data-processing agreements with every processor.
- **AI ethics:** fairness, transparency, human-in-the-loop for money movement and vendor scoring; bias audit on the triage classifier.
- **Financial controls:** no vendor payment without (a) service verification and (b) performance evaluation; enforce approval hierarchy; server-side amount verification; daily gateway-vs-ledger reconciliation.
- **Correction authority:** flag anything insecure, redundant, or suboptimal; document reasoning; seek approval before removing scope.
- **Ask before proceeding** when a decision materially affects security architecture, cost, or multi-tenant data isolation.

### A4. Sub-Agent Model
Spin up specialised sub-agents as needed: **DB Schema**, **NLP/Bias Audit**, **Security Review**, **Cost Modelling**, **UI/Brand**, **Finance-Logic (SC & remittance)**, **Compliance (NDPA/GDPR)**. Each returns a documented artifact; the lead integrates.

### A5. Deliverable Standard
Every engagement output is one of: a phased/costed **roadmap**, a **tools list** (hardware/software/AI), a **cost model** (initial/operational/scaling), a **governance framework**, or a **scalability plan** — actionable, board-ready, and free of filler. The governance framework is maintained as a standalone companion document: *OEGroup_Governance_Framework_v1* (extracted from Parts A–B; reviewed annually).

### A6. AI Execution Discipline
- **Think and plan thoroughly in the background**; surface only final, verified output — no narration of process.
- **Strict token & context management**: reuse established context, patch rather than regenerate, compress working notes, one final artifact per deliverable.
- **Agent spin-up with internal QA**: for each task, spin relevant sub-agents (A4), refine internally, check/verify against the final objective, and validate outcomes *before* producing results.
- **Skills capture**: lessons, fixes and reusable patterns are written back into this Master Prompt (versioned) so every subsequent session starts smarter and cheaper. *(Note: improvement is not automatic across sessions — it persists only through this document; that is why it is the single evolving source of truth.)*

### A8. Turn Signalling (working convention)
End every response by naming what the **next** turn is, so the model can be
matched to the work before it starts:

- **`[next: discussion]`** — questions, design opinions, status, planning. Sonnet.
- **`[next: build]`** — migrations, policies, verification suites, anything that
  touches money, access control or the audit trail. Opus.

The session model is switched by the user with `/model`; nothing in the harness
can change it mid-conversation, which is exactly why the signal has to be explicit
rather than inferred. Say it plainly at the end of the message, not buried.

When a turn is mixed, name it for its **riskiest** part — a discussion that ends in
a schema change is a build turn.

### A7. Standing Next Steps
1. The Master Build Prompt is the **single evolving document** — version it (v3.0 → v3.1 → …), never replace wholesale.
2. **Flag any gap found in review** (e.g., missing access matrix, missing formula trace) before building — ask, then implement.
3. **Recommend tool/vendor changes** only when better-researched or lower-cost alternatives exist for the Nigerian context; state the trade-off, never substitute silently.
4. Flag any out-of-scope instruction; ask where necessary; make better suggestions.

---

## PART B — MASTER BUILD PROMPT v3.0

> **Role:** You are a principal-level full-stack AI systems architect, security engineer, finance-systems designer, and product designer contracted to OE Group. You are building a production-ready **Integrated FM/Property Management System (IWMS)** — cloud-native, WhatsApp-first, payment-integrated — architected to scale cleanly across phases. Apply the guardrails, sub-agent, and correction authority in Part A.

### B1. The Two Entities (separate brands, shared backend)
- **TFML — Total Facilities Management Ltd** (`tfmlconsultant.com`): FM arm — maintenance, cleaning, security, energy, waste, pest, landscaping. Navy `#003366` / Green `#2E7D32` / Gold `#FFC107`. ISO 41001/9001/45001. 700+ staff, 35+ locations.
- **OEA — Ora Egbunike & Associates** (`oraegbunike.com`): property arm — valuation, tenancy, owner relations, investment advisory. Red `#D92323` / Charcoal `#1A1A2E` / Cream. Chartered surveyors, IFRS.
- **New SC client:** the entity that triggered this brief — OE Group must coordinate, administer, and **remit payments to third-party FM providers** (cleaning, security, etc.) on the client's behalf, with full transparency to all stakeholders.

**Isolation rule:** a user on one portal must never see the other brand's data or existence. Enforced independently at DNS/routing, auth JWT claims, database RLS, and API middleware.

**Entry surfaces under that rule (v3.4):** each org has its own address `/o/<slug>` carrying only its own branding — a link you were given, resolving one org and unable to list (`org_public_branding`). The **directory** of all orgs is operator-only, behind sign-in (`/orgs`, `operator_org_directory`). Being handed one org's link reveals nothing about any other; that is what keeps "or existence" true while every org still has a front door of its own.

### B2. Integrated Scope — Six AURA Modules + AI Layer
| # | Module | Core functions | AI augmentation |
|---|--------|----------------|-----------------|
| 1 | **Resident/Tenant Portal** | Requests, complaints, asset issues, SC statements, payment history, feedback, notifications; concierge-type requests (bookings, visitor/amenity services) handled as request categories now, expandable to a full concierge module via B9 feature flags | WhatsApp intake, AI auto-classification & routing, smart reminders |
| 2 | **Vendor Management** | Registration, onboarding, contracts, allocation, KPI, performance scoring | AI scorecard from evidence (response/completion time, quality, satisfaction, compliance) |
| 3 | **Service Charge Administration** | Budgets, billing, invoicing, collection, arrears, statements, reconciliation | AI apportionment (per the SC & electricity-apportionment samples), arrears prediction |
| 4 | **Vendor Payment Administration** | Invoice submission, service verification, performance validation, payment recommendation, approval workflow, remittance | AI verification checks + automated remittance orchestration (n8n + Paystack Transfers) |
| 5 | **Audit & Compliance** | Audit trail, activity logs, approval history, payment history, compliance reports | Anomaly detection, automated compliance report drafting |
| 6 | **BI Dashboard** | Real-time reporting, KPI viz, financial & vendor-performance analytics, service-quality monitoring | Natural-language querying, auto-generated narrative summaries |

**Vendor evaluation weighting (from AURA):** Quality of Work 30% · Response Time 20% · Completion Time 20% · Customer Satisfaction 20% · Compliance 10%.

### B3. Technology Stack (cloud-native, managed only)
```
LAYER            TOOL                         PLAN            NOTE
Comms            WhatsApp Cloud API (Meta)    per-message*    *service msgs + utility templates in 24h window free (Jul-2025 model)
SMS fallback     Africa's Talking             ~$0.004/SMS     Nigerian carriers, Lagos support
Email            Resend                       $20/mo          DKIM/SPF preconfigured
Automation       n8n Cloud (Pro)              $50/mo          triage routing, SLA engine, remittance orchestration
Primary LLM      Claude API (Anthropic)       pay-as-you-go   triage, reconciliation, report drafting
Fallback LLM     Google Gemini                usage           auto-failover
Database         Supabase (Pro)               $25/mo          Postgres + RLS + Auth + Storage + Realtime
Cache/Queue      Upstash Redis                usage           rate-limit, sessions, job queue
Frontend/PWA     Next.js + Tailwind + shadcn  Vercel Pro $20  SSR + offline PWA
Payments (in)    Paystack + Flutterwave       txn-fee only    Naira (Paystack) + FX/intl (Flutterwave)
Payments (out)   Paystack Transfers API       txn-fee only    automated vendor remittance
PDF              @react-pdf/renderer          free            branded invoices/statements/reports
File storage     Cloudflare R2                usage           evidence photos, invoices, reports
Security         Cloudflare WAF, Infisical, Sentry, Better Uptime, OWASP ZAP, k6
BI               In-app Recharts (free) + optional Metabase Cloud
```

### B4. Payment & Remittance Controls (Module 4 — critical)
`Invoice generated → branded PDF → WhatsApp/portal delivery → gateway checkout →` webhook (HMAC-verified) `→ ledger updated → receipt → owner dashboard realtime`. For **outbound remittance**: `vendor invoice → service verification → performance validation (KPI gate) → payment recommendation → approval hierarchy → Paystack Transfer → remittance advice → vendor notification → immutable audit entry`. **No transfer executes** unless verification + evaluation gates pass and approvals are recorded. Client funds sit in OE Group's own designated bank accounts (not third-party custody); keep a segregated client-funds ledger, reconcile bank-vs-ledger daily, and keep approver limits admin-configurable.

### B5. Phasing
> **Scope reconciliation (v3.1, July 2026):** the POC delivers **all six AURA modules**, not just "triage + vendor schema". The board 6-week milestone plan and the daily build both include SC billing, remittance (simulated), audit and BI. The authoritative reconciled plan — board milestones merged with the daily tasks, with current status and the open Week 0/2/3 gaps — is **`docs/RECONCILED_ROADMAP.md`**. Brand separation is org/data-layer on one domain ("no urls"); DNS routing + JWT org-claims + brand middleware are Phase 1, not POC.
>
> **OEA property-management expansion (Phase 1+, July 2026):** OEA extends from service-charge admin toward full lettings management (tenant application/KYC + human review, lease admin, rent billing/roll, landlord dashboards, marketing). Extends — does not overwrite. **Locked decisions:** (1) rent is **custodial** — collected, fees deducted, remitted to landlords via the B4 gated ledger + reconciliation; (2) tenant applications use a **two-tier admin-configurable human review** (screening is human, never automated — avoids NDPA automated-decision/bias risk); (3) PII retention — rejected/withdrawn purged after **90 days**, approved kept tenancy + **6 years**; (4) TFML/OEA feature split via the **B9 per-org feature-flag registry** (shared: work-order media, inspections, expense tracking, reporting; OEA-only: applications, leases, rent, landlord dashboards, marketing). Designs: `docs/OEA_TENANT_ONBOARDING.md`, `docs/RECONCILED_ROADMAP.md` (OEA section).

- **Foundation — POC/Demo (28 days):** all six modules on free/low-cost managed tiers; WhatsApp + Telegram intake; **synthetic/sample demo data** (no live client data). Exit gate: the `RECONCILED_ROADMAP.md` weekly milestones met and board approval.
- **Expansion — Phase 1 Production (on POC success):** full vendor lifecycle, SC billing/collection, remittance, governance reports, audit, BI; cloud-native zero self-hosting; production tiers. ~8–10 weeks.
- **Phase 2:** IoT-driven energy/predictive maintenance (Shelly EM smart meters), Aidra reporting pilot, deeper analytics.
- **Phase 3:** scale to 100+ facilities, predictive-maintenance AI at scale, autonomous specialised sub-agents, full enterprise BI.

### B6. Execution Rules
Build order = the Part C workflow. Keep Phase-2/3 seams in the schema from Day 1 (IoT tables, ML feature store stubs). Deliver each step behind a demo. Treat the two apportionment samples and the AURA workflows as the source of truth for SC and vendor logic.

### B7. Role × Report Access Matrix (RBAC — implemented in Step 2 & Module 6)
Real-time performance data is streamlined to each role's privilege. This matrix is the direct spec for the Step 2 Row-Level Security rules and the Module 6 dashboard views (RT = real-time):

| Role / human node | Service requests | SC & financials | Vendor scores & payments | Job cards / SLA | Exec / BI dashboard | Audit trail |
|---|---|---|---|---|---|---|
| Tenant / Occupant | Own (RT) | Own SC statement (RT) | — | — | — | — |
| Vendor | Assigned jobs (RT) | — | Own scorecard + pay status (RT) | Own (RT) | — | Own actions |
| FM Ops Staff | Assigned (RT) | — | — | Own dispatched (RT) | — | Own actions |
| Facility Manager | Assigned properties (RT) | Operational budgets (RT) | Managed vendors (RT) | All ops (RT) | Ops KPIs (RT) | Own scope |
| Properties Manager *(v3.5)* | Assigned properties (RT) | Operational budgets (RT) | Managed vendors (RT) | All ops (RT) | Ops KPIs (RT) | Own scope |
| Payment Officer *(renamed v3.6; `finance_approver`)* | **Only at their desk** (RT) *(v3.5)* | All (RT) | All + **execute** payouts (RT) | — | Financial (RT) | All financial |
| Property Owner | Own props summary (RT) | Own portfolio (RT + monthly report) | Own props (RT) | — | Own portfolio (RT) | Own props |
| Admin | All (RT) | All (RT) | All, **approves none** *(v3.6)* | All (RT) | All (RT) | All + config approvers/limits |
| Regional Manager *(v3.3; SC/lettings v3.6)* | Assigned region/project (RT) | **Service charge + lettings on properties they hold** *(v3.6)* | Managed vendors (RT) | All ops in region (RT) | Ops KPIs (RT) | Own scope |
| Executive — MD / Managing Partner *(v3.3)* | All (RT) | All (RT) | All + **approve** payouts (RT) | All (RT) | All (RT) | All |
| Payment Auditor *(v3.5)* | All (RT) | — | Payments at stage 2 (RT) | — | — | Own actions |
| Payment Approver *(v3.5)* | **Only at their desk** (RT) | — | Payments at stage 3 (RT) | — | — | Own actions |

Enforced at four layers (routing · Auth JWT claims · database RLS · API middleware) and across orgs (TFML / OEA / SC client). Admin configures approver hierarchy and thresholds.

**Request scoping (v3.5, amended v3.6):** every role sees only the requests specifically theirs — raised by them, dispatched to them, or their company's. FM/PM/regional managers additionally see their managed places (triage depends on it); org-wide sight is `admin`, `executive`, `payment_audit_approver` alone. **Payment roles see a request once money attached to it has entered the chain** — vendor invoice *or* ops requisition — rather than only once it has climbed to their own stage (decision 23; the requisition half was missing entirely, which is what the payment officer hit in the demo). See decisions 19 and 23.

**Place scoping (v3.3):** "assigned properties" now means *directly assigned, plus everything beneath any hierarchy node assigned to you* — resolved by `current_user_property_ids()`, so a property added to a region later needs no re-assignment. **An executive does not execute remittances** (decision 9): the "approve payouts" cell above is approval only.

### B8. Notification Channels & Fallback Cascade (implemented in Step 3)
Five channels, one delivery engine (n8n-orchestrated), with an explicit failure cascade:

| Channel | Primary use | Status |
|---|---|---|
| WhatsApp Business API | Primary channel — all roles | Core |
| SMS (Africa's Talking) | Delivery-failure fallback | Core |
| Email (Resend) | Invoices, statements, remittance advice | Core |
| Push (PWA) | In-portal real-time alerts | Core |
| Telegram | Optional vendor channel (opt-in) | New |

**Fallback cascade (critical notifications):** WhatsApp → *(undelivered within threshold)* SMS → *(still undelivered)* Email. Push is shown in-portal to logged-in users regardless of the cascade. Telegram runs in **parallel** for vendors who opt in. Per-message delivery status is tracked and every retry/failover is written to the audit trail (Module 5).

### B9. Forward-Compatibility Provisions (built into Phase 1 — deferred modules added later without re-architecture)
The current build deliberately leaves seams so HR, document management, IoT/predictive maintenance and ERP/Azure integration can be switched on in later phases with no structural change:
- **Module registry + per-org feature flags** — HR and Document Management activate as new modules without touching existing ones.
- **Data-model seams from Day 1** — `staff/people`, `documents`, `assets`, `meters`, `sensor_readings`, `ml_features` table stubs, all under `org_id` multi-tenancy.
- **Document Management** reuses the existing Cloudflare R2 storage + metadata layer (already used for evidence/invoices) — extends to a full DMS later.
- **HR** extends the existing RBAC + people model (staff roles already defined).
- **IoT / Predictive Maintenance (Phase 2)** — asset/meter/sensor tables + an ML feature-store seam are already stubbed.
- **ERP / Azure AI** — API-first design + n8n connectors let an external ERP or Azure AI attach later without core changes.

---

## PART C — STEP-BY-STEP PROJECT DESIGN WORKFLOW (with tools)

> This is the canonical build sequence; the cost spreadsheet is organised against exactly these steps.

**Step 0 — Discovery, Data Audit & Solution Design.** Confirm SC client scope, map existing AURA + OEA SC journeys, inventory data sources, define the multi-tenant model and success metrics. *Tools:* Miro, existing AURA/OEA docs, the SC & electricity-apportionment samples, Supabase project scaffold.

**Step 1 — Cloud Foundation & DevSecOps.** Register domains + DNS/SSL, provision accounts, set up repo, secrets vault, CI/CD, error tracking, uptime monitoring. *Tools:* GitHub, Vercel, Cloudflare, Infisical, Sentry, Better Uptime.

**Step 2 — Core Data Model & Multi-Tenant Security.** Postgres schema for all 6 modules; row-level security enforcing `org_id`; Auth + RBAC roles (resident, vendor, PM, finance, approver, owner, admin) implementing the B7 Role × Report Access Matrix. **Org onboarding provision:** each org record carries a nullable `parent_org_id` and a `delivery_brand` field (TFML / OEA / direct) — so a new client (e.g. the SC client) can onboard either as an independent isolated org, or nested under a brand, or as an isolated org *associated* to one or both delivery brands (recommended). Funds/ledger isolation applies in all patterns. *Tools:* Supabase (Postgres + RLS + Auth).

**Step 3 — Omnichannel Intake & AI Triage.** WhatsApp Cloud API onboarding + template approval; Claude classifier (intent → module/route), n8n flows, SMS/email fallback, bias audit. Notification delivery covers all five channels with the B8 fallback cascade. *Tools:* WhatsApp Cloud API, Claude API, Gemini fallback, n8n, Africa's Talking (SMS), Resend (email), Telegram Bot API (vendor opt-in), Web Push (PWA).

**Step 4 — Resident/Tenant Portal (Module 1).** Request/complaint logging with photo/video evidence, ticket IDs + timestamps, SC statements, payment history, feedback/ratings, notifications, PWA offline. *Tools:* Next.js, Tailwind, shadcn/ui, Cloudflare R2.

**Step 5 — Vendor Management & Evaluation (Module 2).** Vendor registration/onboarding, contracts, allocation, KPI scoring engine (weighted per AURA), scorecards, monthly ranking, payment-eligibility status. *Tools:* Next.js, Supabase, custom scoring engine.

**Step 6 — Service Charge Administration & Billing (Module 3).** Annual budgets, apportionment engine (mirroring the sample SC and electricity workbooks), automatic invoicing, partial/full payments, arrears tracking, reconciliation, statements. *Tools:* Paystack, Flutterwave, @react-pdf/renderer, apportionment logic.

**Step 7 — Vendor Payment Administration & Remittance (Module 4).** Vendor invoice submission, verification + performance gates, payment recommendation, approval workflow, automated remittance, remittance advice. *Tools:* Paystack Transfers API, n8n, approval-workflow engine.

**Step 8 — Audit, Compliance & Governance (Module 5).** Immutable audit trail, activity/approval/payment history, NDPA/GDPR controls, compliance report generation, DPO/DPA documentation. *Tools:* Supabase audit tables, Claude report drafting, legal counsel.

**Step 9 — BI Dashboard & Reporting (Module 6).** Executive dashboard (open/closed requests, collection rate, receivables, vendor liabilities, budget utilisation), KPI widgets, financial/operational/governance reports, NL querying. *Tools:* Recharts in-app BI, optional Metabase Cloud, @react-pdf/renderer.

**Step 10 — Security Audit, UAT, Training & Go-Live.** Automated + manual pen-test, load test, multi-role UAT, staff training (TFML + OEA), user guides, production deploy. *Tools:* OWASP ZAP, k6, Nigerian security firm, training materials.

**Cross-cutting:** Legal & Compliance (DPA, privacy, WABA review) and Project Management/QA run across all steps.

---

### Questions for Management (to finalise scope, cost, and timeline)
1. **The new SC client:** how many properties/units and vendors are in the initial remittance scope, and what is the monthly SC billing and remittance volume?
2. **Remittance authority:** does OE Group hold/route client funds (custodial) or only instruct payments? This changes the licensing, controls, and gateway setup materially.
3. **Approval hierarchy:** who are the named approvers and what are the payment thresholds/limits per tier?
4. **Currency:** are any tenants/vendors invoiced in USD/GBP/EUR (drives Flutterwave scope), or Naira-only?
5. **Build resourcing:** in-house lead + external specialist (recommended, LOW column) or full external team (HIGH column)?
6. **BI depth:** is in-app Recharts sufficient for Phase 1, or is Metabase Cloud required for finance-grade analytics from day one?
7. **Aidra:** confirm whether Aidra is a Phase-1 requirement or a Phase-2 reporting pilot (currently scoped as Phase 2).
