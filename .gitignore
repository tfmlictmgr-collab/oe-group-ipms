# Dependencies
node_modules
node_modules/

# Environment / secrets
.env
.env.*
*.key
*.pem

# Editor/OS
.DS_Store
Thumbs.db

# Generated per-org artifacts (own thresholds/branding — never a source file)
/docs/generated/

# Next.js
/.next/
/out/
/build
/coverage
/.pnp
.pnp.js
.yarn/install-state.gz
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
.vercel*
*.tsbuildinfo
next-env.d.ts

.vercel
.env*

# Office lock/temp files
~$*
scratchpad/
security/reports/
PR_BODY.md

# Supabase CLI's own local cache (just its own version pin, e.g. "v2.115.0")
supabase/.temp/

# Source documents carrying live tenant/landlord PII. Never in git, and never
# under public/ — Next.js serves that directory statically to anyone.
/private/
public/*.xlsx
public/*.csv
